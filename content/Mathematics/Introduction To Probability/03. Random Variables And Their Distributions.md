## 3.1: Random Variables

In complex probability problems, defining events using set-theoretic notation (unions, intersections, subsets) quickly becomes unwieldy.

**Limitations of pure event notation:**

- Expressing composite quantities (e.g., game duration, difference in wealth) requires long, awkward chains of events.

- Events cannot undergo arithmetic operations (e.g., multiplying an event by a currency exchange rate is undefined).
 
**The Solution:** Define numerical quantities that depend on the outcome of an experiment.

**Example (Gambler's Ruin):** Let $X_k$ be gambler A's wealth after $k$ rounds.

- Wealth of gambler B: $Y_k = N - X_k$ (where $N$ is total wealth)

- Difference in wealth: $X_k - Y_k = 2X_k - N$

- Wealth in euros: $c_k X_k$ (where $c_k$ is the exchange rate)

- Game duration: $R = \min\{n : X_n = 0 \text{ or } Y_n = 0\}$

#### Definition of Random Variable

Given an experiment with sample space $S$, a **random variable (r.v.)** is a function $X$ mapping the sample space $S$ to the real numbers $\mathbb{R}$:
$$X : S \to \mathbb{R}$$
For each outcome $s \in S$, $X$ assigns a deterministic numerical value $X(s)$.
![](Pasted%20image%2020260908093252.png)
- **Notation:** Capital letters ($X, Y, Z$) typically denote random variables.
     
- **Source of Randomness:** The mapping $X(s)$ itself is strictly deterministic. The randomness arises solely from the underlying experiment choosing a sample outcome $s \in S$ according to a probability function $P$.
     
- **Pebble Metaphor:**
    - Outcomes $s_i \in S$ are "pebbles" with masses representing their probabilities $P(s_i)$, summing to $1$.
         
    - An r.v. simply writes a real number label on each pebble.
![](Pasted%20image%2020260908093219.png)
##### Timing & Crystallization

|**Stage**|**Status of X**|**Description**|
|---|---|---|
|**Before the experiment**|Function / Variable|Outcome $s$ is unknown. $X$ has not taken a specific value, but we can compute probabilities of events like $P(X = x)$ or $P(a \le X \le b)$.|
|**After the experiment**|Fixed Real Number|Outcome $s$ is realized, and $X$ "crystallizes" into the static numerical value $X(s)$.|

##### Example: Two Fair Coin Tosses

Experiment: Toss a fair coin twice. Sample space $S = \{HH, HT, TH, TT\}$.

- **Number of Heads ($X$):**
    - $X(HH) = 2, \quad X(HT) = X(TH) = 1, \quad X(TT) = 0$
        
    - Possible values: $\{0, 1, 2\}$
        
- **Number of Tails ($Y$):**
    - $Y = 2 - X$
        
- **Indicator Random Variable ($I$):**
    - An indicator r.v. takes value $1$ if a specific event occurs and $0$ otherwise.
        
    - Let $I$ indicate whether the first toss is Heads:
$$I(HH) = 1, \quad I(HT) = 1, \quad I(TH) = 0, \quad I(TT) = 0$$

**Algebraic Encoding:**

If $S$ is encoded as pairs $(s_1, s_2) \in \{0, 1\}^2$ where $1 = \text{Heads}$ and $0 = \text{Tails}$:
- $X(s_1, s_2) = s_1 + s_2$
     
- $Y(s_1, s_2) = 2 - s_1 - s_2$
     
- $I(s_1, s_2) = s_1$

---

## 3.2: Distributions and Probability Mass Functions

A random variable (r.v.) can be categorized primarily as either discrete or continuous, though hybrid r.v.s also exist (such as generating a discrete r.v. if a coin lands Heads and a continuous r.v. if Tails).

##### Discrete Random Variables and Support

- **Discrete Random Variable:** A random variable $X$ is discrete if there exists a finite list of values $a_1, a_2, \dots, a_n$ or a countably infinite list $a_1, a_2, \dots$ such that:
 $$P(X = a_j \text{ for some } j) = 1$$
    In other words, $X$ can take only one of these values and not any other outside from this set.
    
- **Support:** The finite or countably infinite set of values $x$ such that $P(X = x) > 0$. In most applications, the support is a set of integers.
     
- **Continuous Random Variable (Contrast):** Can take on any real value in an interval (possibly the entire real line).

##### Distribution and the Probability Mass Function (PMF)

The **distribution** of an r.v. specifies the probabilities of all events associated with it. For a discrete r.v., the most natural way to represent the distribution is via a PMF.

- **Definition:** The probability mass function of a discrete r.v. $X$ is the function $p_X$ given by:
 $$p_X(x) = P(X = x)$$
    
    - $p_X(x) > 0$ if $x$ is in the support of $X$.
         
    - $p_X(x) = 0$ otherwise.
 
>[!Note] **Events vs. Random Variables**
> - The expression $X = x$ is shorthand for the event $\{s \in S : X(s) = x\}$, which is a subset of the sample space $S$.
> - Because $\{X = x\}$ is an event, it is mathematically valid to write $P(X = x)$.
> - It is invalid to write $P(X)$; probability applies to events, not to random variables.

#### Examples of PMFs

**Two Fair Coin Tosses**

Sample space: $S = \{HH, HT, TH, TT\}$.

- $X$ = Number of Heads:
    - $p_X(0) = P(X = 0) = 1/4$ (outcome: $TT$)
         
    - $p_X(1) = P(X = 1) = 1/2$ (outcomes: $HT, TH$)
         
    - $p_X(2) = P(X = 2) = 1/4$ (outcome: $HH$)
         
    - $p_X(x) = 0$ for all other $x$.

- $Y = 2 - X$ = Number of Tails:
     - $P(Y = y) = P(2 - X = y) = P(X = 2 - y) = p_X(2 - y)$
         
    - $p_Y(0) = 1/4$, $p_Y(1) = 1/2$, $p_Y(2) = 1/4$, and $0$ otherwise.

- _Key Takeaway:_ $X$ and $Y$ have identical PMFs ($p_X = p_Y$), but they are distinct random variables (different functions mapping $S \to \mathbb{R}$).

- $I$ = Indicator of First Toss Landing Heads:
     - $p_I(0) = P(I = 0) = 1/2$ (outcomes: $TH, TT$)
         
    - $p_I(1) = P(I = 1) = 1/2$ (outcomes: $HH, HT$)
         
    - $p_I(i) = 0$ for all other $i$.
 
 ![](Pasted%20image%2020260908095006.png)

*Caption:* PMFs of $X$ (number of Heads), $Y$ (number of Tails), and $I$ (indicator of Heads on the first toss) in two fair coin tosses.

**Sum of Two Fair 6-Sided Die Rolls**

Let $X$ and $Y$ be the outcomes of two fair 6-sided dice, and $T = X + Y$. The sample space $S = \{(1, 1), (1, 2), \dots, (6, 6)\}$ has 36 equally likely outcomes.

- **Individual Marginal PMFs:** $P(X = j) = 1/6$ for $j \in \{1, 2, \dots, 6\}$ (Discrete Uniform distribution). $Y$ is also Discrete Uniform on $\{1, 2, \dots, 6\}$.

- **Identical Distributions vs. Identical Variables:**
    - $P(X = Y) = 6/36 = 1/6$.
        
    - $7 - X$ and $7 - Y$ also share the same distribution as $X$ (the bottom value of a standard die). However, $7 - X$ is never equal to $X$ in any single roll.

- **PMF of $T$:**
    - $P(T = 2) = P(T = 12) = 1/36$
        
    - $P(T = 3) = P(T = 11) = 2/36$
        
    - $P(T = 4) = P(T = 10) = 3/36$
        
    - $P(T = 5) = P(T = 9) = 4/36$
        
    - $P(T = 6) = P(T = 8) = 5/36$
        
    - $P(T = 7) = 6/36$
        
    - $P(T = t) = 0$ for all other values.

- **Symmetry:** $P(T = t) = P(T = 14 - t)$, because each outcome $\{X = x, Y = y\}$ yielding $T = t$ matches an equally likely outcome $\{X = 7 - x, Y = 7 - y\}$ yielding $T = 14 - t$.

- **Verification:** The support is $\{2, 3, \dots, 12\}$, and $\sum_{t=2}^{12} P(T = t) = 1$.

![](Pasted%20image%2020260908095542.png)

_Caption:_ Triangular PMF of the sum of two fair die rolls showing symmetry around $T = 7$.

**Children in a Randomly Selected U.S. Household**

- Let $X$ be the number of children in a randomly chosen household (taking non-negative integer values).

- $P(X = x)$ is proportional to the count of households with $x$ children (approximated via empirical survey data, e.g., the 2010 General Social Survey).

![700](Pasted%20image%2020260908114021.png)
_Caption:_ PMF of the number of children in a randomly selected U.S. household.

#### Theorem: Criteria for a Valid PMF

Let $X$ be a discrete r.v. with support $x_1, x_2, \dots$ (distinct values). A function $p_X$ is a valid PMF if and only if it satisfies:

1. **Nonnegative:** $p_X(x) > 0$ if $x = x_j$ for some $j$, and $p_X(x) = 0$ otherwise.
    
2. **Sums to 1:**
$$\sum_{j=1}^{\infty} p_X(x_j) = 1$$

_Proof:_ Nonnegativity follows directly from the axiom that probabilities are nonnegative. Summing to 1 follows because $X$ must take on some value in its support, and the events $\{X = x_j\}$ are pairwise disjoint:
$$\sum_{j=1}^{\infty} P(X = x_j) = P\left(\bigcup_{j=1}^{\infty} \{X = x_j\}\right) = P(X = x_1 \text{ or } X = x_2 \text{ or } \dots) = 1$$

_Converse:_ Any function satisfying these two criteria on a set of distinct values represents the PMF of some valid random variable.

**Calculating Probabilities of Subsets from a PMF**

For any subset of real numbers $B \subseteq \mathbb{R}$:
$$P(X \in B) = \sum_{x \in B} p_X(x)$$

**Example:** For the sum of two dice $T$, we want to find $P(1 \le T \le 4)$. The only support values in $[1, 4]$ are $2, 3, 4$:
$$P(1 \le T \le 4) = P(T = 2) + P(T = 3) + P(T = 4) = \frac{1}{36} + \frac{2}{36} + \frac{3}{36} = \frac{6}{36}$$
Visually, finding $P(X \in B)$ corresponds to summing the heights of the vertical bars at all support points lying within $B$. *Knowing the PMF completely determines the distribution of a discrete r.v.*

---

## 3.3: Bernoulli and Binomial Distributions

Named distributions represent probability distributions that occur frequently in probability and statistics. The simplest of these models random variables that take on only two possible values: 0 and 1.

### The Bernoulli Distribution

A random variable $X$ has a Bernoulli distribution with parameter $p$ if:
$$P(X = 1) = p \quad \text{and} \quad P(X = 0) = 1 - p$$
where $0 < p < 1$.

**Notation:** $X \sim \text{Bern}(p)$ (read as "$X$ is distributed as Bernoulli with parameter $p$").

**Parameter:** The value $p$ determines the specific distribution from the family of Bernoulli distributions. Specifying the distribution requires both the name ($\text{Bernoulli}$) and the parameter value ($p$).

**Bernoulli Trial (Story):** An experiment that results in either a "success" or a "failure" (mutually exclusive). A Bernoulli random variable acts as the indicator of success:
$$X = \begin{cases} 1 & \text{if success occurs} \\ 0 & \text{if failure occurs} \end{cases}$$
The parameter $p$ is known as the **success probability**.

#### Indicator Random Variables

The indicator random variable of an event $A$, denoted $I_A$ or $I(A)$, equals $1$ if event $A$ occurs and $0$ otherwise. Any event has an associated indicator variable distributed as Bernoulli:
$$I_A \sim \text{Bern}(p) \quad \text{where } p = P(A)$$

### The Binomial Distribution

**Story:** Perform $n$ independent Bernoulli trials, each having the same success probability $p$. Let $X$ represent the total number of successes. The distribution of $X$ is the Binomial distribution.

**Notation:** $X \sim \text{Bin}(n, p)$, where $n$ is a positive integer and $0 < p < 1$.

**Relationship to Bernoulli:** The Bernoulli distribution is a special case of the Binomial distribution with a single trial:

$$\text{Bern}(p) \equiv \text{Bin}(1, p)$$

**Importance of Stories:** Defining distributions by the physical/experimental setup (the "story") rather than purely by formulas aids in pattern recognition across structurally identical problems, simplifies proofs without heavy computation, and shows connections between different distributions.

**Theorem:(Binomial PMF)** If $X \sim \text{Bin}(n, p)$, then the probability mass function of $X$ is:
$$P(X = k) = \binom{n}{k} p^k (1 - p)^{n-k} \quad \text{for } k \in \{0, 1, \dots, n\}$$
and $P(X = k) = 0$ otherwise. The support of the distribution is the set $\{0, 1, \dots, n\}$.

**Proof:** In $n$ independent Bernoulli trials, any specific, ordered sequence containing exactly $k$ successes and $n - k$ failures occurs with probability:
$$p^k (1 - p)^{n-k}$$
There are $\binom{n}{k}$ ways to choose the locations of the $k$ successes among the $n$ trials.

Multiplying the number of distinct sequences by the probability of any one sequence yields $P(X = k) = \binom{n}{k} p^k (1 - p)^{n-k}$.

This is a valid PMF because all terms are nonnegative and their sum across all $k \in \{0, 1, \dots, n\}$ equals $(p + (1 - p))^n = 1^n = 1$ by the Binomial Theorem. This is also the reason the distribution is called binomial distribution.

**Properties and Shape of the Binomial PMF**

- **Symmetry vs. Skewness:** When $p = 1/2$, the PMF is symmetric. When $p \neq 1/2$, the PMF is skewed.
	
- **Effect of $p$:** For a fixed $n$, larger values of $p$ shift the probability mass toward larger values of $k$, while smaller values of $p$ shift the mass toward lower values of $k$.
    
- **Sum of Masses:** In any PMF plot, the vertical bars sum to 1.    

![](Pasted%20image%2020260908124432.png)
#### Success/Failure Duality and Symmetry Theorems

**Theorem (Distribution of Failures):**

Let $X \sim \text{Bin}(n, p)$, and let $q = 1 - p$ denote the failure probability. Then:
$$n - X \sim \text{Bin}(n, q)$$
**Story Proof:** If $X$ counts the number of successes in $n$ independent Bernoulli trials, $n - X$ represents the number of failures. By interchanging the labels of "success" and "failure," the number of failures is distributed as $\text{Bin}(n, q)$.

**Algebraic Proof:** Let $Y = n - X$. For $k \in \{0, 1, \dots, n\}$:
$$P(Y = k) = P(X = n - k) = \binom{n}{n - k} p^{n - k} q^k = \binom{n}{k} q^k p^{n - k}$$
which matches the $\text{Bin}(n, q)$ PMF.    

**Corollary (Symmetry for $p = 1/2$):**

If $X \sim \text{Bin}(n, 1/2)$ with $n$ even, the distribution of $X$ is symmetric about $n/2$:
$$P\left(X = \frac{n}{2} + j\right) = P\left(X = \frac{n}{2} - j\right) \quad \text{for all integers } j \ge 0$$
**Proof:** Since $p = 1 - p = 1/2$, $n - X \sim \text{Bin}(n, 1/2)$, meaning $P(X = k) = P(n - X = k) = P(X = n - k)$. Substituting $k = n/2 + j$ yields $n - k = n/2 - j$.


**Example:** (Two Fair Coin Tosses Continues) For an experiment of two fair coin tosses:

- $X$ (number of Heads) $\sim \text{Bin}(2, 1/2)$.
    
- $Y = 2 - X$ (number of Tails) $\sim \text{Bin}(2, 1/2)$.
    
- $I$ (indicator of Heads on the first toss) $\sim \text{Bern}(1/2)$.
    
- Because $p = 1/2$ and $n = 2$ (even), $X$ and $Y$ have the same distribution, both symmetric about $n/2 = 1$.

---

## 3.4: Hypergeometric Distribution

When drawing $n$ balls from an urn containing $w$ white and $b$ black balls:

- **With replacement:** The number of white balls follows a $\text{Bin}(n, w/(w + b))$ distribution because draws are independent Bernoulli trials.
	
- **Without replacement:** The trials become dependent, and the number of white balls follows a Hypergeometric distribution.

#### Story and Definition

**Story 3.4.1 (Hypergeometric Distribution):** Consider an urn with $w$ white balls and $b$ black balls. We draw $n$ balls out of the urn at random without replacement such that all $\binom{w+b}{n}$ samples are equally likely. Let $X$ be the number of white balls in the sample.

**Notation:** $X \sim \text{HGeom}(w, b, n)$ with parameters:
- $w$: Total number of white items ("successes" in population)
	
- $b$: Total number of black items ("failures" in population)
	
- $n$: Sample size drawn without replacement

![](Pasted%20image%2020260908152647.png)
#### Theorem: Hypergeometric PMF  

If $X \sim \text{HGeom}(w, b, n)$, the probability mass function of $X$ is:
$$P(X = k) = \frac{\binom{w}{k}\binom{b}{n - k}}{\binom{w + b}{n}}$$
for integers $k$ satisfying:
$$0 \le k \le w \quad \text{and} \quad 0 \le n - k \le b$$
and $P(X = k) = 0$ otherwise.

**Proof:** To obtain $P(X = k)$, count the ways to draw exactly $k$ white balls and $n - k$ black balls without regard to order.

- If $k > w$ or $n - k > b$, drawing such a sample is impossible. Otherwise, by the multiplication rule, there are $\binom{w}{k}\binom{b}{n - k}$ favorable samples.
	
- The total number of equally likely samples of size $n$ from $w + b$ balls is $\binom{w + b}{n}$.

By the naive definition of probability, $P(X = k) = \frac{\binom{w}{k}\binom{b}{n - k}}{\binom{w + b}{n}}$.
 
**Validity Check:** The PMF is nonnegative, and by **Vandermonde's identity**, the numerator summed over all possible $k$ equals $\binom{w + b}{n}$, confirming the probabilities sum to 1:
$$\sum_{k} \binom{w}{k}\binom{b}{n - k} = \binom{w + b}{n}$$

>[!Note]
>If you consider the order, then also we get the same answer. This is because after choosing a set of $k$ white balls and $n-k$ black balls, the number of arrangements they can be obtained is $n!$. This term will show up in both the numerator and the denominator and get cancelled.

**The Two-Tags Interpretation**

The Hypergeometric distribution generalizes beyond physical urns. The fundamental structure relies on classifying a population using **two sets of tags**:

1. **First set of tags:** An intrinsic property of each item (e.g., white vs. black).
    
2. **Second set of tags:** Whether the item is selected (e.g., sampled vs. not sampled).

At least one set of tags must be assigned completely at random. Then $X \sim \text{HGeom}(w, b, n)$ represents the count of **twice-tagged items** (items possessing both target tags).

>[!Note]
>The distribution is called Hyper-Geometric because it resembles the Hyper-Geometric series in mathematics.
>
>In general, the geometric series is the one where the ratio of consecutive terms is constant i.e. $\frac{a_{k+1}}{a_{k}} = r$. 
>
>Later mathematicians started to study series where the ratio of consecutive terms is a rational function of the index i.e. $\frac{a_{k+1}}{a_{k}} = F(k)$. Because these series went beyond the geometric (hyper in Greek means *over* or *beyond*), these series were called Hyper-Geometric.
>
>Now consider the ration of PMF of consecutive terms in this series, we get,
>$$\frac{P(X=k+1)}{P(X=k)} = \frac{(W-k) \cdot (n-k)}{(k+1) \cdot (b-n+k+1)}$$
>
>Hence, the name Hyper-Geometric distribution.

#### Applied Examples

**Elk Capture-Recapture**

A forest has $N$ total elk. $m$ elk are captured, tagged, and released. Later, $n$ elk are recaptured at random (assuming equal likelihood and that captured elk do not learn to avoid recapture). The number of tagged elk in the recaptured sample follows:
$$X \sim \text{HGeom}(m, N - m, n)$$
**Aces in a Poker Hand**

A five-card hand ($n = 5$) drawn at random from a standard 52-card deck ($w = 4$ aces, $b = 48$ non-aces). The number of aces follows $\text{HGeom}(4, 48, 5)$.

Probability of getting exactly 3 aces:
$$P(X = 3) = \frac{\binom{4}{3}\binom{48}{2}}{\binom{52}{5}} \approx 0.0017$$

Let's see how the two tags interpretation fairs in these examples,

|**Story**|**First Set of Tags (Target)**|**First Set of Tags (Other)**|**Second Set of Tags (Target)**|**Second Set of Tags (Other)**|**Twice-Tagged Variable (X)**|
|---|---|---|---|---|---|
|**Urn**|White|Black|Sampled|Not sampled|White and sampled|
|**Elk**|Tagged ($m$)|Untagged ($N - m$)|Recaptured ($n$)|Not recaptured|Tagged and recaptured|
|**Cards**|Ace ($4$)|Not ace ($48$)|In hand ($5$)|Not in hand|Ace and in hand|

#### Theorem: Parameter Symmetry

The distributions $\text{HGeom}(w, b, n)$ and $\text{HGeom}(n, w + b - n, w)$ are identical:
$$\text{HGeom}(w, b, n) \equiv \text{HGeom}(n, w + b - n, w)$$
**Story Proof:**

- Let $X \sim \text{HGeom}(w, b, n)$ be the number of white balls in a sample of size $n$ (treating white/black as the 1st tag and sampled/unsampled as the 2nd tag).

- Let $Y \sim \text{HGeom}(n, w + b - n, w)$ be the number of sampled balls among the $w$ white balls (treating sampled/unsampled as the 1st tag and white/black as the 2nd tag).

- Both $X$ and $Y$ count the exact same entity: balls that are simultaneously white and sampled. Thus, they follow the same distribution.

**Algebraic Verification:**
$$
P(X = k) = \frac{\binom{w}{k}\binom{b}{n - k}}{\binom{w + b}{n}} = \frac{w!\,b!\,n!\,(w + b - n)!}{k!\,(w - k)!\,(n - k)!\,(b - n + k)!\,(w + b)!}
$$
$$
P(Y = k) = \frac{\binom{n}{k}\binom{w + b - n}{w - k}}{\binom{w + b}{w}} = \frac{n!\,(w + b - n)!\,w!\,b!}{k!\,(n - k)!\,(w - k)!\,(b - n + k)!\,(w + b)!}
$$
Both expressions are algebraically identical.

#### Comparison: Binomial vs. Hypergeometric

**Similarities:** Both are discrete distributions with support over integers $\{0, 1, \dots, n\}$ representing the total number of "successes" in $n$ trials.

**Core Difference (Independence):**

- **Binomial:** Sampling is done **with replacement**; trials are independent.
	
- **Hypergeometric:** Sampling is done **without replacement**; trials are **dependent**. (For example, observing a tagged elk on the first draw reduces the probability of getting a tagged elk on the second draw).

---

## 3.5: Discrete Uniform Distribution

The Discrete Uniform distribution models an experiment where a single number is chosen uniformly at random from a finite set of possibilities, directly embodying the naive definition of probability.

#### Story and Definition

**Story (Discrete Uniform Distribution):** Let $C$ be a finite, nonempty set of numbers. Choose one of these numbers uniformly at random (every value in $C$ is equally likely). Let $X$ be the chosen number.

**Notation:**
$$X \sim \text{DUnif}(C)$$
where the parameter is the set $C$ itself.

#### PMF and Event Probabilities

**Probability Mass Function:** The PMF of discrete uniform distribution directly follows from the naive definition of probability,
$$P(X = x) = \frac{1}{\vert{}C\vert{}} \quad \text{for } x \in C$$
and $P(X = x) = 0$ otherwise (satisfying $\sum_{x \in C} P(X = x) = 1$).

**Subset Probability:** For any subset $A \subseteq C$:
$$P(X \in A) = \frac{\vert{}A\vert{}}{\vert{}C\vert{}}$$
Questions involving a Discrete Uniform distribution directly reduce to counting problems.    

#### Case Study: Random Slips of Paper

Consider a hat containing $100$ slips of paper labeled $1, 2, \dots, 100$ (with no duplicates). Five slips are drawn, one at a time.

**Scenario 1: Sampling With Replacement (Independent Draws)**

- **Count of slips with value $\ge 80$:** Values $\ge 80$ form the set $\{80, 81, \dots, 100\}$, containing $21$ values. The probability of success on any draw is $p = 21/100 = 0.21$. Because draws are independent Bernoulli trials, the total count follows:
	$$
	\text{Bin}(5, 0.21)
	$$
- **Distribution of the $j$-th draw ($X_j$, for $1 \le j \le 5$):** By symmetry, every slip is equally likely to be selected on any given draw.
	$$
	X_j \sim \text{DUnif}(\{1, 2, \dots, 100\})
	$$
- **Probability that 100 is drawn at least once:** Using the complement rule and independence of draws:
	$$
	\begin{align}
	P(X_j = 100 \text{ for at least one } j) & = 1 - P(X_1 \ne 100, \dots, X_5 \ne 100) \\
	 & = 1 - P(X_1 \ne 100)\cdots P(X_5 \ne 100) \\
	 & = 1 - \left(\frac{99}{100}\right)^5 \approx 0.049
	\end{align}
	$$

**Scenario 2: Sampling Without Replacement (Dependent Draws)**

- **Count of slips with value $\ge 80$:** Tagged/target slips ($w$): $21$ slips with values $\ge 80$. Untagged slips ($b$): $79$ slips with values $< 80$. Sample size ($n$): $5$. The distribution is Hypergeometric:
	$$
	\text{HGeom}(21, 79, 5)
	$$
- **Distribution of the $j$-th draw ($Y_j$, for $1 \le j \le 5$):** Unconditionally (evaluated prior to observing any draws), the $j$-th draw is equally likely to be any of the $100$ slips:
	$$
	Y_j \sim \text{DUnif}(\{1, 2, \dots, 100\})
	$$
	_Simultaneous Draw Perspective:_ Imagine five people reaching into the hat at the exact same moment to each draw one slip. Let $Z_j$ be the slip drawn by person $j$. By symmetry, each person draws a uniformly random slip: $Z_j \sim \text{DUnif}(\{1, 2, \dots, 100\})$. While $Y_1, \dots, Y_5$ are dependent, marginally/unconditionally each is Discrete Uniform.

- **Probability that 100 is drawn in the sample:** Because draws are made without replacement, the events $Y_1 = 100, \dots, Y_5 = 100$ are pairwise disjoint:
	$$
	P(Y_j = 100 \text{ for some } j) = P(Y_1 = 100) + \dots + P(Y_5 = 100) = 5 \times \frac{1}{100} = 0.05
	$$
	_Sanity Check:_ Choosing $5$ slips first and then randomly writing numbers $1$ to $100$ on all slips gives an identical $5/100$ probability that the number $100$ lands on one of the $5$ chosen slips.

**Key Comparison: With vs. Without Replacement**

- **Comparison of Probabilities:**
$$P(\text{draw } 100 \text{ with replacement}) \approx 0.049 < P(\text{draw } 100 \text{ without replacement}) = 0.05$$
    Sampling without replacement eliminates duplicate selections, making it strictly more effective when searching for a specific target. The values remain close because the probability of redrawing the same slip across only 5 draws is small.

- **Generalization:** If $k$ slips are drawn without replacement ($0 \le k \le 100$), the probability of drawing the number $100$ is:
$$\frac{k}{100}$$
    In the extreme case where $k = 100$ (drawing all slips), the probability equals $100/100 = 1$.

---

## 3.6 Cumulative Distribution Functions

The cumulative distribution function (CDF) is another function that describes the distribution of a random variable. Unlike the probability mass function (PMF), which is defined only for discrete random variables, the CDF is defined for all random variables (discrete, continuous, and hybrid).

**Definition:** The cumulative distribution function (CDF) of a random variable $X$ is the function $F_X$ given by:
$$F_X(x) = P(X \le x)$$
When unambiguous, the subscript is often dropped and written simply as $F(x)$.

#### Converting Between PMF and CDF (Discrete Case)

For discrete random variables, the PMF and CDF contain equivalent information and can be converted back and forth:
![](Pasted%20image%2020260908230750.png)
**PMF to CDF:** Sum the PMF over all support values less than or equal to $x$:
$$
F_X(x) = P(X \le x) = \sum_{k \le x} P(X = k)
$$
_Example:_ For $X \sim \text{Bin}(4, 1/2)$, evaluating the CDF at $x = 1.5$:
$$
P(X \le 1.5) = P(X = 0) + P(X = 1) = \left(\frac{1}{2}\right)^4 + 4\left(\frac{1}{2}\right)^4 = \frac{5}{16}
$$
Visually, the CDF value at $x$ equals the sum of the heights of the PMF vertical bars for all values $\le x$.

**CDF to PMF:** The CDF of a discrete r.v. consists of flat horizontal regions connected by vertical jumps:

- **Jump Height:** The height of a jump in the CDF at point $x$ equals $P(X = x)$ (the PMF value at $x$).

- **Flat Regions:** Correspond to values outside the support of $X$, where the PMF is equal to $0$.

#### Theorem: Properties of a Valid CDF

Any valid cumulative distribution function $F$ must satisfy three criteria:

1. **Increasing (Non-decreasing):** If $x_1 \le x_2$, then: $F(x_1) \le F(x_2)$
    
2. **Right-Continuous:** $F$ is continuous except possibly for jump discontinuities. At every point $a$, the function is continuous from the right:
	$$
	F(a) = \lim_{x \to a^+} F(x)
	$$
    _(In plots, jumps have an open dot at the bottom and a closed dot at the top)._
    
3. **Limits at Negative and Positive Infinity:**
	$$
	\lim_{x \to -\infty} F(x) = 0 \quad \text{and} \quad \lim_{x \to \infty} F(x) = 1
	$$
    _(The CDF may reach $0$ and $1$ exactly, or approach them asymptotically)._

**Proof for Discrete r.v. with Nonnegative Integer Support $\{0, 1, 2, \dots\}$**

- **Property 1 (Increasing):** The event $\{X \le x_1\}$ is a subset of the event $\{X \le x_2\}$. By the monotonicity of probability:
	$$
	P(X \le x_1) \le P(X \le x_2)
	$$
- **Property 2 (Right-Continuous):** Since $X$ is integer-valued:
	$$
	P(X \le x) = P(X \le \lfloor x \rfloor)
	$$
    where $\lfloor x \rfloor$ is the floor function (greatest integer $\le x$). For any sufficiently small $b > 0$ such that $a + b < \lfloor a \rfloor + 1$, we have $F(a + b) = F(a)$. Because $F(x)$ remains constant just to the right of $a$:
	$$
	\lim_{x \to a^+} F(x) = \lim_{ h \to 0 } F(a+h) = F(a)
	$$
    
- **Property 3 (Limits at Infinity):** Since the support contains only nonnegative integers, $F(x) = 0$ for all $x < 0$, giving $\lim_{x \to -\infty} F(x) = 0$. As $x \to \infty$:
	$$
	\lim_{x \to \infty} F(x) = \lim_{x \to \infty} P(X \le \lfloor x \rfloor) = \lim_{x \to \infty} \sum_{n=0}^{\lfloor x \rfloor} P(X = n) = \sum_{n=0}^{\infty} P(X = n) = 1
	$$
#### Summary: Three Ways to Express a Distribution

1. **PMF (Probability Mass Function):** Specific to discrete random variables. Typically the easiest to work with for discrete distributions since it avoids summation operations required by the CDF.

2. **CDF (Cumulative Distribution Function):** Universal framework defined for all types of random variables. Directly interchangeable with the PMF for discrete variables.

3. **Story:** Specifies the underlying experiment or physical mechanism generating the distribution. Contains the same core mathematical information as the PMF/CDF while enabling intuitive, clean proofs without tedious algebraic calculations.

---

## 3.7: Functions of Random Variables

Applying a mathematical function $g : \mathbb{R} \to \mathbb{R}$ to a random variable $X$ produces a new random variable, denoted $g(X)$ (such as $X^2$, $e^X$, or $\sin(X)$).

**Example (Basketball Match):** Two teams ($A$ and $B$) play a 7-game series. If games are independent and teams are evenly matched, the number of wins for team $A$ is $X \sim \text{Bin}(7, 1/2)$.

- Let $g(x) = 7 - x \implies g(X) = 7 - X$ is an r.v. representing the wins for team $B$.

- Let $h(x) = 1$ if $x \ge 4$ and $0$ if $x < 4 \implies h(X)$ is an indicator r.v. for team $A$ winning the series.
 
#### Formal Definition and Composition

- **Definition (Function of an r.v.):** For an experiment with sample space $S$, an r.v. $X$, and a function $g : \mathbb{R} \to \mathbb{R}$, $g(X)$ is the random variable that maps outcome $s$ to $g(X(s))$ for all $s \in S$.
    
- **Composition:** $g(X)$ represents the composition of functions $X$ and $g$ ($s \mapsto X(s) \mapsto g(X(s))$)—meaning "first apply $X$, then apply $g$." If $X$ crystallizes to a specific number $x$, then $g(X)$ crystallizes to $g(x)$.

![](Pasted%20image%2020260909094052.png)
![736](Pasted%20image%2020260909094127.png)

#### Finding the PMF of $Y = g(X)$

**Case 1: $g$ is a One-to-One Function**

If $g$ is one-to-one, each possible value of $X$ maps to a unique value of $Y$. The support of $Y$ consists of all $g(x)$ where $x$ is in the support of $X$, and probabilities transfer directly:
$$
P(Y = g(x)) = P(g(X) = g(x)) = P(X = x)
$$

| **x**    | **P(X=x)** | **y**    | **P(Y=y)** |
| -------- | ---------- | -------- | ---------- |
| $x_1$    | $p_1$      | $g(x_1)$ | $p_1$      |
| $x_2$    | $p_2$      | $g(x_2)$ | $p_2$      |
| $x_3$    | $p_3$      | $g(x_3)$ | $p_3$      |
| $\vdots$ | $\vdots$   | $\vdots$ | $\vdots$   |

_Strategy:_ To find the PMF of an unfamiliar r.v., attempt to express it as a one-to-one function of an r.v. with a known distribution.

**Example (Random Walk Position):** A particle starts at $0$ and takes $n$ independent steps on a number line, each step moving $+1$ (right) or $-1$ (left) with equal probability $1/2$. Let $Y$ be the position after $n$ steps.

- Let $X$ be the number of steps to the right $\implies X \sim \text{Bin}(n, 1/2)$.

- If $X = j$, the particle moves $j$ steps right and $n - j$ steps left:
	$$
	Y = j - (n - j) = 2X - n
	$$
- Because $g(x) = 2x - n$ is one-to-one, the support of $Y$ is $\{-n, 2 - n, 4 - n, \dots, n\}$.
 
- For any integer $k \in [-n, n]$ such that $n + k$ is even:
	$$
	P(Y = k) = P\left(X = \frac{n + k}{2}\right) = \binom{n}{\frac{n + k}{2}}\left(\frac{1}{2}\right)^n
	$$

**Case 2: $g$ is Not One-to-One**

When multiple values of $x$ map to the same value $y$, sum the probabilities of all candidate $x$ values that yield $y$.

**Theorem (PMF of $g(X)$):** Let $X$ be a discrete r.v. and $g : \mathbb{R} \to \mathbb{R}$. The support of $g(X)$ is the set of all $y$ such that $g(x) = y$ for at least one $x$ in the support of $X$, and its PMF is:
$$
P(g(X) = y) = \sum_{x : g(x) = y} P(X = x)
$$
**Example (Distance from Origin in a Random Walk):** Let $D = \vert{}Y\vert{}$ be the distance from the origin after $n$ steps (with $n$ even). The absolute value function is not one-to-one:
- $D = 0 \iff Y = 0$

- $D = k \iff \{Y = k\} \cup \{Y = -k\}$ for $k \in \{2, 4, \dots, n\}$

- By symmetry, $P(Y = k) = P(Y = -k)$, giving:
$$
P(D = 0) = \binom{n}{n/2}\left(\frac{1}{2}\right)^n
$$
$$
P(D = k) = P(Y = k) + P(Y = -k) = 2\binom{n}{\frac{n + k}{2}}\left(\frac{1}{2}\right)^n \quad \text{for } k \in \{2, 4, \dots, n\}
$$

#### Functions of Multiple Random Variables

**Definition (Function of Two r.v.s):** Given an experiment with sample space $S$, if $X$ and $Y$ map $s \in S$ to $X(s)$ and $Y(s)$, then $g(X, Y)$ is the r.v. mapping $s$ to $g(X(s), Y(s))$.

- _Sample Space Assumption:_ $X$ and $Y$ must be defined on the same sample space $S$. If originally defined on separate spaces $S_1$ and $S_2$, redefine them on the product space $S = S_1 \times S_2$.

- **Crystallization:** If $X = x$ and $Y = y$, then $g(X, Y)$ crystallizes to $g(x, y)$ (e.g., $X + Y$ crystallizes to $x + y$, and $\max(X, Y)$ crystallizes to $\max(x, y)$).


**Example: Maximum of Two Fair Die Rolls**

Let $X$ and $Y$ be the outcomes of two fair 6-sided dice ($36$ equally likely outcomes).

| **Outcome (s)** | **X** | **Y** | **max(X,Y)** |
| --------------- | ----- | ----- | ------------ |
| $(1, 2)$        | 1     | 2     | 2            |
| $(1, 6)$        | 1     | 6     | 6            |
| $(2, 5)$        | 2     | 5     | 5            |
| $(3, 1)$        | 3     | 1     | 3            |
| $(4, 3)$        | 4     | 3     | 4            |
| $(5, 4)$        | 5     | 4     | 5            |
| $(6, 6)$        | 6     | 6     | 6            |

Tabulating across all 36 pairs in a $6 \times 6$ grid yields the PMF:
$$
\begin{align}
P(\max(X, Y) = 1) &  = 1/36 \\
P(\max(X, Y) = 2) & = 3/36 \\
P(\max(X, Y) = 3) & = 5/36 \\
P(\max(X, Y) = 4) & = 7/36 \\
P(\max(X, Y) = 5) & = 9/36 \\
P(\max(X, Y) = 6) & = 11/36
\end{align}
$$
_Alternative Calculation (Decomposition):_
$$
\begin{align}
P(\max(X, Y) = 5) & = P(X = 5, Y \le 4) + P(X \le 4, Y = 5) + P(X = 5, Y = 5) \\
 & = 2P(X = 5, Y \le 4) + \frac{1}{36} = 2\left(\frac{4}{36}\right) + \frac{1}{36} = \frac{9}{36}
\end{align}
$$

#### Category Errors and "Sympathetic Magic"

A **category error** is an error resulting from confusing fundamentally different classes of mathematical objects: distributions, random variables, events, and numbers.

**Sympathetic Magic:** The specific category error of confusing a random variable with its distribution.

_"The word is not the thing; the map is not the territory."_ — Alfred Korzybski

The distribution is merely the blueprint or map describing the random variable. Distinct random variables defined on entirely different sample spaces can have identical distributions.

**Two Classical Pitfalls of Sympathetic Magic:**

1. **Multiplying a PMF by a Constant (e.g., attempting to find the PMF of $2X$):**
      
    - _The Error:_ Multiplying the PMF values of $X$ by 2 (which invalidates the PMF because the probabilities would sum to 2).
        
    - _The Correction:_ If $X$ takes values $x_j$ with probability $p_j$, then $2X$ takes values $2x_j$ with probability $p_j$. The transformation represents a **horizontal stretch** of the support, not a vertical stretch of the probabilities. (e.g., support $\{0, 1, 2, 3, 4\}$ becomes $\{0, 2, 4, 6, 8\}$).

![](Pasted%20image%2020260909103337.png)

2. **Assuming Identical Distribution Implies Equality ($P(X = Y) = 1$):**
      
    - Having the same distribution does not imply two random variables are equal, or that they ever equal each other.
        
    - _Toss Example 1:_ In a single fair coin toss, let $X$ indicate Heads and $Y = 1 - X$ indicate Tails. Both $X, Y \sim \text{Bern}(1/2)$, but $P(X = Y) = 0$ (the event $X = Y$ is impossible).
        
    - _Toss Example 2:_ Let $Z$ be the indicator of Heads on an independent second toss. $Z \sim \text{Bern}(1/2)$, but $P(Z = X) = P(\{HH, TT\}) = 1/2 \ne 1$.

---

## 3.8 Independence of Random Variables

Two random variables $X$ and $Y$ are independent if knowing the value of one provides no information about the value of the other.

#### Definitions of Independence  

**Definition (Two Random Variables):** Random variables $X$ and $Y$ are independent if:
$$
P(X \le x, Y \le y) = P(X \le x)P(Y \le y) \quad \text{ for all } x, y \in \mathbb{R}
$$
**Discrete Equivalent:** For discrete r.v.s, this is equivalent to:
$$
P(X = x, Y = y) = P(X = x)P(Y = y)
$$
for all $x$ in the support of $X$ and all $y$ in the support of $Y$.

**Definition (Many Random Variables):** Random variables $X_1, \dots, X_n$ are independent if:
$$
P(X_1 \le x_1, \dots, X_n \le x_n) = P(X_1 \le x_1)\dots P(X_n \le x_n) \quad \text{for all } x_1, \dots, x_n \in \mathbb{R}
$$
- **Infinite Collection:** An infinite set of r.v.s is independent if every finite subset is independent.

- **Contrast with Event Independence:** Unlike events—which require checking pairwise independence, triplet independence, up to $n$-way independence—r.v. independence requires a single product equation, but that equation must hold across **all** possible combinations of $x_1, \dots, x_n$ (an infinite number of conditions). A failure at even a single point disproves independence.

- **Pairwise vs. Mutual Independence:** Mutual independence implies pairwise independence (letting all other $x_k \to \infty$). However, pairwise independence does **not** imply mutual independence.

>[!Note]
>Let's see how the single product equation is the same as the $2^{n}-n-1$ equations that we need in the case of events. It all comes down to the fact that that single equation must be true for all $(x_{1},x_{2},\dots,x_{n}) \in R^{n}$ and the following equation,
>$$
>\lim_{ x \to \infty } P(X\leq x) = 1 = P(S) \implies \lim_{ x \to \infty } X\leq x=S
>$$
>
>So, let's say we want to prove the pairwise independence of $X_{1}$ and $X_{2}$, then we will make $x_{3},\dots,x_{n} \to \infty$. In the LHS we will have
>$$
>\begin{align}\text{LHS} & = \lim_{ x_{3},\dots,x_{n} \to \infty } P(X_{1}\leq x_{1},X_{2}\leq x_{2},X_{3}\leq x_{3},\dots,X_{n}\leq x_{n}) \\  & = P(X_{1}\leq x_{1},X_{2}\leq x_{2},S) \\ & = P(X_{1}\leq x_{1},X_{2}\leq x_{2})\end{align}
>$$
>Similarly, in RHS, we have
>$$
>\begin{align}\text{RHS} & = P(X_{1}\leq x_{1})\cdot P(X_{2}\leq x_{2})\cdot \lim_{ x_{3} \to \infty }  P(X_{3}\leq x_{3})\dots \lim_{ x_{n} \to \infty } P(X_{n}\leq x_{n}) \\  & = P(X_{1}\leq x_{1})\cdot P(X_{2}\leq x_{2})\cdot 1\dots 1 \\  & = P(X_{1}\leq x_{1}) \cdot P(X_{2} \leq x_{2})\end{align}
>$$
>Hence, we can prove pairwise, triplets, quadruplets, quintuplets i.e. every subset as independent with the single product equation.

**Example: Sum and Difference of Two Dice Rolls**

Let $X$ and $Y$ be the outcomes of two independent rolls of a fair die. The random variables $X + Y$ and $X - Y$ are dependent:
$$
P(X + Y = 12, X - Y = 1) = 0 \ne P(X + Y = 12)P(X - Y = 1) = \frac{1}{36} \cdot \frac{5}{36}
$$
Knowing the total sum is 12 forces the difference to be 0, so the variables convey information about each other.

**Theorem: Functions of Independent Random Variables**

If $X$ and $Y$ are independent random variables, then any function of $X$ is independent of any function of $Y$ (e.g., $X^2$ is independent of $Y^4$).

Although, we are not gonna prove this theorem, but the theorem sounds intuitive enough (I think).

#### Independent and Identically Distributed (i.i.d.)

- **Definition:** Random variables are **i.i.d.** if they are mutually independent and share the exact same probability distribution (same PMF or CDF).

**Distinction: Independent ("i.") vs. Identically Distributed ("i.d.")**

Independence and identical distribution are separate, orthogonal concepts:

| **Relationship**                              | **Description**                                                              | **Example**                                                                                                                  |
| --------------------------------------------- | ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| **Independent & Identically Distributed**     | Share the same PMF/CDF and provide no information about each other.          | Two independent rolls of a fair die ($X$ and $Y$).                                                                           |
| **Independent & Not Identically Distributed** | Provide no information about each other, but follow different distributions. | Roll of a die ($X$) vs. the closing price of the Dow Jones index next month ($Y$).                                           |
| **Dependent & Identically Distributed**       | Share the same distribution, but convey information about each other.        | Number of Heads ($X$) and Tails ($Y = n - X$) in the same $n$ tosses of a fair coin (both are $\text{Bin}(n, 1/2)$).         |
| **Dependent & Not Identically Distributed**   | Follow different distributions and convey information about each other.      | Indicator of the majority party retaining control of the House ($X$) vs. party favorability ratings prior to election ($Y$). |

#### Binomial Representations and Properties

**Theorem (Sum of i.i.d. Bernoullis):**
If $X \sim \text{Bin}(n, p)$, representing the total successes in $n$ independent Bernoulli trials with parameter $p$:
$$
X = X_1 + X_2 + \dots + X_n
$$
where $X_i \overset{\text{i.i.d.}}{\sim} \text{Bern}(p)$ ($X_i = 1$ if trial $i$ succeeds, $0$ otherwise).

**Proof:** Each individual r.v. $X_{i}$ tells you whether that trial ended in success or not, then you count how many of the r.v.s are giving $X_{i} = 1$, that exactly what the sum is doing, hence the sum represents the binomial distribution.

**Theorem (Sum of Independent Binomials):**
If $X \sim \text{Bin}(n, p)$ and $Y \sim \text{Bin}(m, p)$ are independent, then:
$$
X + Y \sim \text{Bin}(n + m, p)
$$
**Proof 1 (Law of Total Probability):** Condition on $X = j$ and apply independence:
$$
\begin{align}
P(X + Y = k) & = \sum_{j=0}^k P(X + Y = k \mid X = j)P(X = j) \\
 & = \sum_{j=0}^k P(Y = k - j)P(X = j) \\
 & = \sum_{j=0}^k \binom{m}{k - j} p^{k - j} q^{m - (k - j)} \binom{n}{j} p^j q^{n - j} \\
 & = p^k q^{n + m - k} \sum_{j=0}^k \binom{m}{k - j}\binom{n}{j}
\end{align}
$$
By Vandermonde's identity, the sum equals $\binom{n + m}{k}$, yielding the $\text{Bin}(n + m, p)$ PMF:
$$
\binom{n + m}{k} p^k q^{n + m - k}
$$
**Proof 2 (Representation):** Write $X = \sum_{i=1}^n X_i$ and $Y = \sum_{j=1}^m Y_j$, where all $X_i, Y_j$ are i.i.d. $\text{Bern}(p)$. Then $X + Y$ is the sum of $n + m$ i.i.d. $\text{Bern}(p)$ variables, which is $\text{Bin}(n + m, p)$.

**Proof 3 (Story):** $X$ counts successes in $n$ trials and $Y$ counts successes in $m$ independent trials with identical success probability $p$. The sum $X + Y$ is the total successes across all $n + m$ trials, matching the definition of $\text{Bin}(n + m, p)$.    

#### Conditional Independence and Conditional PMFs

**Definition (Conditional Independence):** $X$ and $Y$ are conditionally independent given $Z$ if for all $x, y \in \mathbb{R}$ and all $z$ in the support of $Z$:
$$
P(X \le x, Y \le y \mid Z = z) = P(X \le x \mid Z = z) P(Y \le y \mid Z = z)
$$
**Discrete Case:**
$$
P(X = x, Y = y \mid Z = z) = P(X = x \mid Z = z) P(Y = y \mid Z = z)
$$

**Definition (Conditional PMF):** For discrete r.v.s $X$ and $Z$, $P(X = x \mid Z = z)$ viewed as a function of $x$ for a fixed $z$ is the conditional PMF of $X$ given $Z = z$.

#### Independence vs. Conditional Independence

Independence does **not** imply conditional independence, nor does conditional independence imply unconditional independence.

**1. Independence Does Not Imply Conditional Independence**

- **Example (Matching Pennies):** Players A and B toss fair pennies independently. Let $X = 1$ (Heads) or $-1$ (Tails) for A, and $Y = 1$ or $-1$ for B (random signs). Let $Z = XY$ ($1$ if pennies match, $-1$ otherwise). Unconditionally, $X$ and $Y$ are independent. Conditioned on $Z = 1$, we know $X = Y$, making them dependent.
    
- **Example (Two Friends):** Let $X$ indicate Alice calling, $Y$ indicate Bob calling (assumed independent), and $Z$ indicate exactly one calling. Conditioned on $Z = 1$, $Y = 1 - X$, making them completely dependent.

**2. Conditional Independence Does Not Imply Independence**

- **Example (Mystery Opponent):** You play two games against one of two identical twins: an evenly matched twin (win probability $1/2$) or a weaker twin (win probability $3/4$). Let $Z = 1$ indicate the evenly matched twin, and $X, Y$ indicate wins in games 1 and 2.
    - Given $Z = 1$, $X$ and $Y$ are i.i.d. $\text{Bern}(1/2)$.
        
    - Given $Z = 0$, $X$ and $Y$ are i.i.d. $\text{Bern}(3/4)$.
        
    - Thus, $X$ and $Y$ are conditionally independent given $Z$.
        
    - Unconditionally, $X$ and $Y$ are dependent: observing $X = 1$ provides evidence that the opponent is the weaker twin, which raises the probability of winning the second game:
		$$
		P(Y = 1 \mid X = 1) > P(Y = 1)
		$$

---

## 3.9: Connections Between Binomial and Hypergeometric Distributions

The Binomial and Hypergeometric distributions are connected in two fundamental ways:

1. **Conditioning:** Conditioning independent Binomial random variables on their sum produces a Hypergeometric distribution.
    
2. **Limit:** Taking the limit of a Hypergeometric distribution as the population size grows to infinity produces a Binomial distribution.

#### From Binomial to Hypergeometric: Conditioning (Fisher's Exact Test)

Consider testing whether two groups have the same probability of an outcome (e.g., disease status in women vs. men):

- Let $X \sim \text{Bin}(n, p_1)$ be the number of diseased women out of $n$.
     
- Let $Y \sim \text{Bin}(m, p_2)$ be the number of diseased men out of $m$.
     
- Assume $X$ and $Y$ are independent.
     
- **Null Hypothesis:** $p_1 = p_2 = p$. Under this hypothesis, $X \sim \text{Bin}(n, p)$, $Y \sim \text{Bin}(m, p)$, and their sum follows $X + Y \sim \text{Bin}(n + m, p)$.

The test conditions on both the row and column sums of the observed $2 \times 2$ contingency table, treating $n$, $m$, and the total observed cases $r = X + Y$ as fixed:

| **Status**     | **Women** | **Men**     | **Total**   |
| -------------- | --------- | ----------- | ----------- |
| **Disease**    | $x$       | $r - x$     | $r$         |
| **No disease** | $n - x$   | $m - r + x$ | $n + m - r$ |
| **Total**      | $n$       | $m$         | $n + m$     |

**Derivation of the Conditional PMF:**

Using Bayes' rule and the independence of $X$ and $Y$:
$$
P(X = x \mid X + Y = r) = \frac{P(X + Y = r \mid X = x)P(X = x)}{P(X + Y = r)} = \frac{P(Y = r - x)P(X = x)}{P(X + Y = r)}
$$
Substituting the Binomial PMFs under the null hypothesis:
$$
P(X = x \mid X + Y = r) = \frac{\binom{m}{r - x} p^{r - x} (1 - p)^{m - r + x} \binom{n}{x} p^x (1 - p)^{n - x}}{\binom{n + m}{r} p^r (1 - p)^{n + m - r}} = \frac{\binom{n}{x}\binom{m}{r - x}}{\binom{n + m}{r}}
$$
This is the PMF of an $\text{HGeom}(n, m, r)$ distribution.

**Connection to the Elk Story:** Regard women as tagged elk ($n$) and men as untagged elk ($m$). Under the null hypothesis, the $r$ diseased individuals are equally likely to be any subset of size $r$ from the total population $n + m$. $X$ represents the number of "tagged elk" (women) among the $r$ "recaptured" individuals (diseased), which follows $\text{HGeom}(n, m, r)$.

**Elimination of the Parameter $p$:** The conditional distribution of $X$ given $X + Y = r$ does not depend on the unknown parameter $p$. Once the total number of diseased people $r$ is known, the problem reduces to sampling without replacement from a fixed population of $r$ diseased and $n + m - r$ healthy individuals.

**Theorem: Conditioning Independent Binomials**

If $X \sim \text{Bin}(n, p)$, $Y \sim \text{Bin}(m, p)$, and $X$ is independent of $Y$, then the conditional distribution of $X$ given $X + Y = r$ is:
$$
X \mid (X + Y = r) \sim \text{HGeom}(n, m, r)
$$

#### From Hypergeometric to Binomial: Limiting Distribution

**Theorem (Binomial as a Limit):**

If $X \sim \text{HGeom}(w, b, n)$ and the total population $N = w + b \to \infty$ while the success ratio $p = w/(w + b)$ remains fixed (with $q = 1 - p = b/(w + b)$), then the PMF of $X$ converges to the $\text{Bin}(n, p)$ PMF:
$$
P(X = k) \to \binom{n}{k} p^k q^{n - k}
$$
**Proof:** Applying the parameter symmetry of the Hypergeometric distribution ($\text{HGeom}(w, b, n) \equiv \text{HGeom}(n, w + b - n, w)$):
$$
P(X = k) = \frac{\binom{w}{k}\binom{b}{n - k}}{\binom{w + b}{n}} = \binom{n}{k} \frac{\binom{w + b - n}{w - k}}{\binom{w + b}{w}}
$$
Expanding the combinations into factorials and products:
$$
\begin{align}
 & = \binom{n}{k} \frac{w!\,b!\,(w + b - n)!}{(w - k)!\,(b - n + k)!\,(w + b)!} \\
 & = \binom{n}{k} \frac{[w(w - 1)\dots(w - k + 1)] \cdot [b(b - 1)\dots(b - n + k + 1)]}{(w + b)(w + b - 1)\dots(w + b - n + 1)}
\end{align}
$$
Dividing each term in the numerator and denominator by $N = w + b$:
$$
= \binom{n}{k} \frac{\left[p\left(p - \frac{1}{N}\right)\dots\left(p - \frac{k - 1}{N}\right)\right] \cdot \left[q\left(q - \frac{1}{N}\right)\dots\left(q - \frac{n - k - 1}{N}\right)\right]}{1\left(1 - \frac{1}{N}\right)\left(1 - \frac{2}{N}\right)\dots\left(1 - \frac{n - 1}{N}\right)}
$$
Taking the limit as $N \to \infty$:

- The denominator approaches $1 \times 1 \times \dots \times 1 = 1$.

- The numerator approaches $p^k \cdot q^{n - k}$.

- The expression converges to $\binom{n}{k} p^k q^{n - k}$, which is the $\text{Bin}(n, p)$ PMF.

**Story Intuition and Practical Approximation**

- **Sampling Mechanisms:** The Binomial distribution models sampling $n$ items **with replacement**, whereas the Hypergeometric distribution models sampling $n$ items **without replacement**.
    
- **Large Population Approximation:** When the population size $N = w + b$ is very large relative to the sample size $n$, removing a few items without replacement causes a negligible change in the composition of the remaining population. Therefore, sampling without replacement behaves essentially like sampling with replacement:
$$
\text{HGeom}(w, b, n) \approx \text{Bin}\left(n, \frac{w}{w + b}\right) \quad \text{when } N \gg n
$$
- **Connection to the Birthday Problem:** Even though drawing with replacement from a large urn can yield duplicate selections (e.g., drawing $1{,}200$ items out of $1{,}000{,}000$ with replacement yields roughly a $51\%$ chance of at least one duplicate), the Binomial approximation remains accurate because the vast majority of items in the sample are drawn only once.

---

## Exercise

### $n$ vs. $n+1$ Coin Toss Symmetry

**(25)** Alice flips a fair coin $n$ times and Bob flips another fair coin $n + 1$ times, resulting in independent random variables:
$$
X \sim \operatorname{Bin}\left(n, \frac{1}{2}\right) \quad \text{and} \quad Y \sim \operatorname{Bin}\left(n + 1, \frac{1}{2}\right)
$$
1. **(a)** Show that: $P(X < Y) = P(n - X < n + 1 - Y)$
2. **(b)** Compute $P(X < Y)$.

**(Ans)** There are two ways to solve this problem, one is using story and other is using hardcore algebra (;-)

##### Story Solution

**(a)** The result follows from the fact that $n-X$ and $n+1-Y$ have the same distributions as $X$ and $Y$. This can be seen as follows,
$$
\begin{align}
P(n-X = k) & = P(X=n-k)  \\
 & = \binom{n}{n-k} \frac{1}{2^{n}} \\
 & = \binom{n}{k} \frac{1}{2^{n}} \\
 & = P(X = k) \\ \\
P(n+1-Y = k) & = P(Y=n+1-k) \\
 & = \binom{n+1}{n+1-k} \frac{1}{2^{n+1}} \\
 & = \binom{n+1}{k} \frac{1}{2^{n+1}} \\
 & = P(Y=k)
\end{align}
$$
In terms of story, we can imagine as $X$ being the number of heads Alice gets and $Y$ is the number of heads Bob gets, then $n-X$ is the number of tails Alice gets and $n+1-Y$ is the number of tails Bob gets. And then by symmetry both of them must have the same distribution.

**(b)** From we know **(a)** we know that $P(X<Y) = P(n-X<n+1-Y)$, we can simplify RHS as follows,
$$
P(n-X<n+1-Y) = P(Y < X+1) = P(Y \leq X) \quad \text{(}\because \text{X and Y are integers)}
$$
We know that $X<Y$ and $Y\leq X$ are complements and they have the same probability, which means that,
$$
P(X<Y) = P(Y\leq X) = \frac{1}{2}
$$
##### Algebraic Solution (Only For Part (a))

**Step 1: Expand the LHS using LOTP on $X$**
$$
P(X < Y) = \sum_{k=0}^n P(X = k) P(Y > k)
$$
Since $X \sim \operatorname{Bin}\left(n, \frac{1}{2}\right)$ and $Y \sim \operatorname{Bin}\left(n+1, \frac{1}{2}\right)$ are independent:
$$
\begin{align}
P(X = k) & = \binom{n}{k} \left(\frac{1}{2}\right)^n \\
P(Y > k) & = \sum_{j=k+1}^{n+1} \binom{n+1}{j} \left(\frac{1}{2}\right)^{n+1}
\end{align}
$$
Multiplying them together gives:
$$
P(X < Y) = \frac{1}{2^{2n+1}} \sum_{k=0}^n \binom{n}{k} \sum_{j=k+1}^{n+1} \binom{n+1}{j}
$$
**Step 2: Expand the RHS using LOTP on $X$**

First, rearrange the inequality inside the probability:
$$
n - X < n + 1 - Y \iff Y < X + 1 \iff Y \le X
$$
_(since $X$ and $Y$ take integer values)_

Now condition on $X = k$ and expanding using LOTP we get the following expression,
$$
P(n - X < n + 1 - Y) = \frac{1}{2^{2n+1}} \sum_{k=0}^n \binom{n}{k} \sum_{j=0}^k \binom{n+1}{j}
$$
**Step 3: Transform the LHS Sum into the RHS Sum**

Take the double sum in the LHS:
$$
\sum_{k=0}^n \binom{n}{k} \sum_{j=k+1}^{n+1} \binom{n+1}{j}
$$
Apply a change of variables (re-indexing) using binomial symmetry:

1. **Outer index:** Let $u = n - k$. As $k$ goes from $0$ to $n$, $u$ runs from $n$ down to $0$ (covering the same range $0 \le u \le n$). By symmetry:
$$
\binom{n}{k} = \binom{n}{n-u} = \binom{n}{u}
$$
1. **Inner index:** Let $v = (n + 1) - j$. When $j = k + 1$, the upper limit for $v$ is:
	$$
	v = (n + 1) - (k + 1) = n - k = u
	$$
	When $j = n + 1$, the lower limit for $v$ is:
	$$
	v = (n + 1) - (n + 1) = 0
	$$
	By symmetry:
	$$
	\binom{n+1}{j} = \binom{n+1}{(n+1)-v} = \binom{n+1}{v}
	$$
Replacing $j$ with $v$, the inner sum becomes:
$$
\sum_{j=k+1}^{n+1} \binom{n+1}{j} = \sum_{v=0}^u \binom{n+1}{v}
$$
Putting both substitutions together:
$$
\sum_{k=0}^n \binom{n}{k} \sum_{j=k+1}^{n+1} \binom{n+1}{j} = \sum_{u=0}^n \binom{n}{u} \sum_{v=0}^u \binom{n+1}{v}
$$
Since $u$ and $v$ are dummy variables, renaming $u \to k$ and $v \to j$ gives:
$$
\frac{1}{2^{2n+1}} \sum_{k=0}^n \binom{n}{k} \sum_{j=0}^k \binom{n+1}{j}
$$
This is identical to the RHS expression, completing the algebraic proof.

>[!Note]
>Another way to look at it is that in RHS, we have $\binom{n}{k}$ being multiplied with first $k+1$ terms i.e. $\sum_{j=0}^{k}\binom{n+1}{j}$. Which means that $\binom{n}{n-k}$ is being multiplied with the sum of first $n-k+1$ terms.
>
>In LHS, $\binom{n}{k}$ is being multiplied with with the last $(n+1) - (k+1) + 1 = n-k+1$ terms, but we know that $\binom{n}{k} = \binom{n}{n-k}$, also the sum of first $n-k+1$ terms is the same as the sum of the last $n-k+1$ terms.
>
>Hence, for every term in RHS, there is a matching term in LHS.

---

### Benford’s Law as a Valid PMF

**(6)** Benford’s law states that in a very large variety of real-life data sets, the first digit approximately follows a particular distribution with about a 30% chance of a 1, an 18% chance of a 2, and in general
$$
P(D = j) = \log_{10}\left(\frac{j + 1}{j}\right), \quad \text{for } j \in \{1, 2, 3, \dots, 9\},
$$
where $D$ is the first digit of a randomly chosen element. Check that this is a valid PMF (using properties of logs, not with a calculator).

**(Ans)** To be a valid PMF on the support $\{1, 2, \dots, 9\}$, the distribution must satisfy two properties:

1. **Non-negativity:** For any $j \in \{1, 2, \dots, 9\}$, the ratio $\frac{j+1}{j} > 1$. Because the base-10 logarithm of any number strictly greater than 1 is positive:
$$
P(D = j) = \log_{10}\left(\frac{j+1}{j}\right) > 0.
$$
1. **Sum to 1:** Using the logarithm property $\log_{10}(a/b) = \log_{10}(a) - \log_{10}(b)$, the sum telescopes:
$$
\begin{align}
\sum_{j=1}^9 P(D = j) & = \sum_{j=1}^9 \left(\log_{10}(j+1) - \log_{10}(j)\right) \\
 & = (\log_{10} 2 - \log_{10} 1) + (\log_{10} 3 - \log_{10} 2) + \dots + (\log_{10} 10 - \log_{10} 9) \\
 & = \log_{10}(10) - \log_{10}(1) = 1 - 0 = 1.
\end{align}
$$
Both conditions are met, so this is a valid PMF.

---  

### Mixture of Cumulative Distribution Functions

**(9)** Let $F_1$ and $F_2$ be CDFs, $0 < p < 1$, and $F(x) = p F_1(x) + (1 - p)F_2(x)$ for all $x$.

**(a)** Show directly that $F$ has the properties of a valid CDF. The distribution defined by $F$ is called a mixture of the distributions defined by $F_1$ and $F_2$.

**(b)** Consider creating an r.v. in the following way. Flip a coin with probability $p$ of Heads. If the coin lands Heads, generate an r.v. according to $F_1$; if the coin lands Tails, generate an r.v. according to $F_2$. Show that the r.v. obtained in this way has CDF $F$.

**(Ans)** **(a)** A valid CDF must satisfy three conditions:

- **Non-decreasing:** Let $x \le y$. Since $F_1$ and $F_2$ are non-decreasing, $F_1(x) \le F_1(y)$ and $F_2(x) \le F_2(y)$. Because $p > 0$ and $1 - p > 0$:
	$$
	F(x) = p F_1(x) + (1 - p) F_2(x) \le p F_1(y) + (1 - p) F_2(y) = F(y).
	$$
- **Limits at $\pm \infty$:**
	$$
	\lim_{x \to -\infty} F(x) = p \lim_{x \to -\infty} F_1(x) + (1 - p) \lim_{x \to -\infty} F_2(x) = p(0) + (1 - p)(0) = 0
	$$
	$$
	\lim_{x \to \infty} F(x) = p \lim_{x \to \infty} F_1(x) + (1 - p) \lim_{x \to \infty} F_2(x) = p(1) + (1 - p)(1) = 1
	$$
- **Right-continuity:** For any $x_0$, taking the right-hand limit as $x \to x_0^+$:
	$$
	\lim_{x \to x_0^+} F(x) = p \lim_{x \to x_0^+} F_1(x) + (1 - p) \lim_{x \to x_0^+} F_2(x) = p F_1(x_0) + (1 - p) F_2(x_0) = F(x_0)
	$$
    Thus, $F$ satisfies all criteria of a valid CDF.


**(b)** Let $I$ be the indicator that the coin lands Heads ($P(I = 1) = p$, $P(I = 0) = 1 - p$), and let $X$ be the resulting random variable. By the Law of Total Probability:
$$
P(X \le x) = P(X \le x \mid I = 1)P(I = 1) + P(X \le x \mid I = 0)P(I = 0) = F_1(x) p + F_2(x) (1 - p) = F(x)
$$
Hence, the generated variable $X$ has CDF $F$.

---

### Catching Independent Typos

**(33)** A book has $n$ typos. Two proofreaders, Prue and Frida, independently read the book. Prue catches each typo with probability $p_1$ and misses it with probability $q_1 = 1 - p_1$, independently, and likewise for Frida, who has probabilities $p_2$ of catching and $q_2 = 1 - p_2$ of missing each typo. Let $X_1$ be the number of typos caught by Prue, $X_2$ be the number caught by Frida, and $X$ be the number caught by at least one of the two proofreaders.

**(a)** Find the distribution of $X$.

**(b)** For this part only, assume that $p_1 = p_2$. Find the conditional distribution of $X_1$ given that $X_1 + X_2 = t$.

**(Ans)**
**(a)** A given typo is missed by both readers with probability $q_1 q_2 = (1 - p_1)(1 - p_2)$. Therefore, the probability that a typo is detected by at least one proofreader is:
$$
p^* = 1 - (1 - p_1)(1 - p_2) = p_1 + p_2 - p_1 p_2.
$$
Since typos are detected independently, $X$ is the sum of $n$ independent Bernoulli($p^*$) trials:
$$
X \sim \text{Bin}(n, p_1 + p_2 - p_1 p_2).
$$
**(b)** Let $p_1 = p_2 = p$. Prue's detections $X_1 \sim \text{Bin}(n, p)$ and Frida's detections $X_2 \sim \text{Bin}(n, p)$ are independent, so their sum is $X_1 + X_2 \sim \text{Bin}(2n, p)$. For valid values of $k$:
$$
P(X_1 = k \mid X_1 + X_2 = t) = \frac{P(X_1 = k, X_2 = t - k)}{P(X_1 + X_2 = t)} = \frac{\binom{n}{k} p^k (1-p)^{n-k} \binom{n}{t-k} p^{t-k} (1-p)^{n-(t-k)}}{\binom{2n}{t} p^t (1-p)^{2n-t}} = \frac{\binom{n}{k}\binom{n}{t-k}}{\binom{2n}{t}}.
$$
This is the Hypergeometric distribution:
$$
X_1 \mid (X_1 + X_2 = t) \sim \text{HGeom}(n, n, t),
$$
with support $\max(0, t - n) \le k \le \min(n, t)$.

---  

### Statistics Majors in a Random Sample 

**(34)** There are $n$ students at a certain school, of whom $X \sim \text{Bin}(n, p)$ are Statistics majors. A simple random sample of size $m$ is drawn (“simple random sample” means sampling without replacement, with all subsets of the given size equally likely).

**(a)** Find the PMF of the number of Statistics majors in the sample, using the law of total probability (don’t forget to say what the support is). You can leave your answer as a sum.

**(b)** Give a story proof derivation of the distribution of the number of Statistics majors in the sample; simplify fully.

Hint: Does it matter whether the students declare their majors before or after the random sample is drawn?

**(Ans)** Let $Y$ denote the number of Statistics majors in the sample of size $m$. The support of $Y$ is $\{0, 1, \dots, m\}$.

**(a)** Conditioning on the total number of majors in the school $X = k$: given $X = k$, $Y$ follows a Hypergeometric distribution $\text{HGeom}(k, n - k, m)$. By the Law of Total Probability:
$$
P(Y = j) = \sum_{k=j}^{n - m + j} P(Y = j \mid X = k) P(X = k) = \sum_{k=j}^{n - m + j} \frac{\binom{k}{j}\binom{n-k}{m-j}}{\binom{n}{m}} \binom{n}{k} p^k (1-p)^{n-k},
$$
for $j \in \{0, 1, \dots, m\}$.

(b) **Story Proof:** Because simple random sampling selects students uniformly at random without regard to their major, it does not matter whether students declare their majors before or after the sample is chosen.

Imagine drawing the sample of $m$ students first, and then having each of the $m$ chosen students independently declare their major with probability $p$ of choosing Statistics. The number of Statistics majors among them is simply the sum of $m$ i.i.d. Bernoulli($p$) trials:
$$
Y \sim \text{Bin}(m, p).
$$
Thus, the fully simplified PMF is:
$$
P(Y = j) = \binom{m}{j} p^j (1 - p)^{m - j}, \quad \text{for } j \in \{0, 1, \dots, m\}.
$$

---

### Undetected Errors via Parity Check

**(37)** A message is sent over a noisy channel. The message is a sequence $x_1, x_2, \dots, x_n$ of $n$ bits ($x_i \in \{0, 1\}$). Since the channel is noisy, there is a chance that any bit might be corrupted, resulting in an error (a 0 becomes a 1 or vice versa). Assume that the error events are independent. Let $p$ be the probability that an individual bit has an error ($0 < p < 1/2$). Let $y_1, y_2, \dots, y_n$ be the received message.

To help detect errors, the $n$th bit is reserved for a parity check: $x_n$ is defined to be 0 if $x_1 + \dots + x_{n-1}$ is even, and 1 if it is odd. When the message is received, the recipient checks whether $y_n$ has the same parity as $y_1 + \dots + y_{n-1}$. If the parity is wrong, the recipient knows that at least one error occurred; otherwise, the recipient assumes that there were no errors.

**(a)** For $n = 5$, $p = 0.1$, what is the probability that the received message has errors which go undetected?

**(b)** For general $n$ and $p$, write down an expression (as a sum) for the probability that the received message has errors which go undetected.

**(c)** Give a simplified expression, not involving a sum of a large number of terms, for the probability that the received message has errors which go undetected.

**(Ans)** The parity-check equation requires $\sum_{i=1}^n x_i \equiv 0 \pmod 2$. Since $y_i = x_i \oplus e_i$, where $e_i \in \{0, 1\}$ indicates an error, the received sum satisfies $\sum_{i=1}^n y_i \equiv \sum_{i=1}^n e_i \pmod 2$. Thus, errors go undetected if and only if the total number of errors $K \sim \text{Bin}(n, p)$ is non-zero and even ($K \in \{2, 4, 6, \dots\}$).

**(a)** For $n = 5$ and $p = 0.1$:
$$
\begin{align}
P(\text{undetected}) & = \binom{5}{2} (0.1)^2 (0.9)^3 + \binom{5}{4} (0.1)^4 (0.9)^1 \\
 & = 10(0.01)(0.729) + 5(0.0001)(0.9) = 0.0729 + 0.00045 = 0.07335.
\end{align}
$$

**(b)** For general $n$ and $p$:
$$
P(\text{undetected}) = \sum_{\substack{k=2 \\ k \text{ even}}}^n \binom{n}{k} p^k (1-p)^{n-k}.
$$
**(c)** By the binomial theorem:
$$
a + b = \sum_{k=0}^n \binom{n}{k} p^k (1-p)^{n-k} = ((1-p) + p)^n = 1,
$$
$$
a - b = \sum_{k=0}^n \binom{n}{k} (-p)^k (1-p)^{n-k} = ((1-p) - p)^n = (1 - 2p)^n,
$$
where $a = P(K \text{ is even})$ and $b = P(K \text{ is odd})$. Adding both expressions yields:
$$
a = \frac{1 + (1 - 2p)^n}{2}.
$$
Subtracting the probability of zero errors $P(K = 0) = (1 - p)^n$:
$$
P(\text{undetected}) = a - (1 - p)^n = \frac{1 + (1 - 2p)^n}{2} - (1 - p)^n.
$$

---

### Shifted Day of the Week

**(42)** Let $X$ be a random day of the week, coded so that Monday is 1, Tuesday is 2, etc. (so $X$ takes values 1, 2, . . . , 7, with equal probabilities). Let $Y$ be the next day after $X$ (again represented as an integer between 1 and 7). Do $X$ and $Y$ have the same distribution? What is $P(X < Y)$?

**(Ans)** **Distribution:** $X$ is uniformly distributed on $\{1, 2, \dots, 7\}$, with $P(X = k) = 1/7$ for each $k$. $Y$ maps each day to the following day: $Y = X + 1$ for $X \in \{1, \dots, 6\}$ and $Y = 1$ for $X = 7$.

- For $j \in \{2, 3, \dots, 7\}$, $P(Y = j) = P(X = j - 1) = 1/7$.

- For $j = 1$, $P(Y = 1) = P(X = 7) = 1/7$.

Because $P(Y = j) = 1/7$ for all $j \in \{1, \dots, 7\}$, **$X$ and $Y$ have the exact same distribution.**

**Probability $P(X < Y)$:** The condition $X < Y$ holds whenever $X \in \{1, 2, 3, 4, 5, 6\}$ (where $Y = X + 1$). The only case where $X \not< Y$ is when $X = 7$ (Sunday), giving $Y = 1$. Thus:
$$
P(X < Y) = P(X \in \{1, 2, 3, 4, 5, 6\}) = \frac{6}{7}.
$$

---

### Identically Distributed Variables and Order Probabilities

**(a)** Is it possible to have two r.v.s $X$ and $Y$ such that $X$ and $Y$ have the same distribution but $P(X < Y) \ge p$, where:
- $p = 0.9$?
- $p = 0.99$?
- $p = 0.9999999999999$?
- $p = 1$?
For each, give an example showing it is possible, or prove it is impossible.

**(b)** Consider the same question as in Part (a), but now assume that $X$ and $Y$ are independent. Do your answers change?

**(Ans)** **(a)**
- **For $p = 0.9$, $p = 0.99$, and $p = 0.9999999999999$:** **Possible.** Generalize Problem 42: let $X \sim \text{Uniform}(\{1, 2, \dots, n\})$, and let $Y = X + 1$ for $X < n$, with $Y = 1$ when $X = n$. Both $X$ and $Y$ are uniformly distributed on $\{1, 2, \dots, n\}$. Here, $X < Y$ holds for all $X \in \{1, \dots, n - 1\}$, so:
	$$
	P(X < Y) = \frac{n - 1}{n} = 1 - \frac{1}{n}.
	$$
	- For $p = 0.9$: take $n = 10 \implies P(X < Y) = 0.9$.
		
	- For $p = 0.99$: take $n = 100 \implies P(X < Y) = 0.99$.
		
	- For $p = 1 - 10^{-13}$: take $n = 10^{13} \implies P(X < Y) = 1 - 10^{-13}$.

- **For $p = 1$:** **Impossible.** If $P(X < Y) = 1$, then $Y - X > 0$ almost surely. For any bounded strictly increasing function $g$ (such as $g(t) = \arctan(t)$), $g(Y) - g(X) > 0$ almost surely, which implies $E[g(Y) - g(X)] > 0$. However, because $X$ and $Y$ share the exact same distribution, $E[g(Y)] = E[g(X)]$, giving $E[g(Y) - g(X)] = 0$, a contradiction.

(b) **Yes, the answers change: it is impossible for all given values of $p$.** If $X$ and $Y$ are independent and identically distributed, then by symmetry:
$$
P(X < Y) = P(Y < X).
$$
Since $P(X < Y) + P(Y < X) + P(X = Y) = 1$:
$$
2P(X < Y) = 1 - P(X = Y) \le 1 \implies P(X < Y) \le \frac{1}{2}.
$$
Because $P(X < Y)$ cannot exceed $1/2 = 0.5$, it can never reach $0.9$, $0.99$, $0.9999999999999$, or $1$.

---

### XOR of Bernoulli Random Variables

**(44)** For $x$ and $y$ binary digits (0 or 1), let $x \oplus y$ be 0 if $x = y$ and 1 if $x \ne y$.

**(a)** Let $X \sim \text{Bern}(p)$ and $Y \sim \text{Bern}(1/2)$, independently. What is the distribution of $X \oplus Y$?

**(b)** With notation as in (a), is $X \oplus Y$ independent of $X$? Is $X \oplus Y$ independent of $Y$? Be sure to consider both the case $p = 1/2$ and the case $p \ne 1/2$.

**(c)** Let $X_1, \dots, X_n$ be i.i.d. $\text{Bern}(1/2)$. For each nonempty subset $J \subseteq \{1, 2, \dots, n\}$, let $Y_J = \bigoplus_{j \in J} X_j$. Show that $Y_J \sim \text{Bern}(1/2)$ and that these $2^n - 1$ r.v.s are pairwise independent, but not independent.

**(Ans)** **(a)** Conditioning on $X$:
$$
\begin{align}
P(X \oplus Y = 1) & = P(X \oplus Y = 1 \mid X = 0)P(X = 0) + P(X \oplus Y = 1 \mid X = 1)P(X = 1) \\
 & = P(Y = 1)(1 - p) + P(Y = 0)p = \frac{1}{2}(1 - p) + \frac{1}{2}p = \frac{1}{2}.
\end{align}
$$
Because it takes values in $\{0, 1\}$ with probability $1/2$, $X \oplus Y \sim \text{Bern}(1/2)$.

**(b)** **Independence with $X$:**
$$
P(X \oplus Y = 1 \mid X = 0) = P(Y = 1) = \frac{1}{2}, \quad P(X \oplus Y = 1 \mid X = 1) = P(Y = 0) = \frac{1}{2}.
$$
Since $P(X \oplus Y = 1 \mid X = x) = P(X \oplus Y = 1) = 1/2$ for all $x$, $X \oplus Y$ and $X$ are **independent for all $p$**.

**Independence with $Y$:**
$$
P(X \oplus Y = 1 \mid Y = 0) = P(X = 1) = p, \quad P(X \oplus Y = 1 \mid Y = 1) = P(X = 0) = 1 - p.
$$
Independence requires $P(X \oplus Y = 1 \mid Y = y) = 1/2$ for all $y$, which holds if and only if $p = 1/2$.
- If $p = 1/2$: $X \oplus Y$ and $Y$ are **independent**.
- If $p \ne 1/2$: $X \oplus Y$ and $Y$ are **not independent**.

**(c)**
1. **Marginal Distribution:** Proceed by induction on $\vert{}J\vert{}$. For $\vert{}J\vert{} = 1$, $Y_{\{j\}} = X_j \sim \text{Bern}(1/2)$. Assuming it holds for $\vert{}J\vert{} = k$, adding one more independent bit $X_{k+1}$ gives $Y_{J \cup \{k+1\}} = Y_J \oplus X_{k+1}$, which by part (a) is distributed as $\text{Bern}(1/2)$. Thus, $Y_J \sim \text{Bern}(1/2)$ for every non-empty $J$.

2. **Pairwise Independence:** Let $J \ne K$ be two distinct non-empty subsets.
	- If $J \cap K = \emptyset$, $Y_J$ and $Y_K$ depend on disjoint sets of independent variables and are therefore independent.
	- If $J \cap K \ne \emptyset$, since $J \ne K$, at least one symmetric difference is non-empty; without loss of generality, let $J \setminus K \ne \emptyset$. Choose some index $i^* \in J \setminus K$, and write:
	$$
	Y_J = X_{i^*} \oplus \left(\bigoplus_{j \in J \setminus \{i^*\}} X_j\right).
	$$
	Because $X_{i^*}$ is entirely absent from $Y_K$ and from the rest of $Y_J$, applying part (b) with $X_{i^*} \sim \text{Bern}(1/2)$ shows that $Y_J$ is independent of $Y_K$. Thus, all $2^n - 1$ variables are pairwise independent.

3. **Not Mutually Independent:** Consider three distinct non-empty subsets $J_1 = \{1\}$, $J_2 = \{2\}$, and $J_3 = \{1, 2\}$. Then:
	$$
	Y_{J_3} = X_1 \oplus X_2 = Y_{J_1} \oplus Y_{J_2}.
	$$
    Given the values of $Y_{J_1}$ and $Y_{J_2}$, $Y_{J_3}$ is determined with certainty ($P(Y_{J_3} = Y_{J_1} \oplus Y_{J_2} \mid Y_{J_1}, Y_{J_2}) = 1 \ne 1/2$). Hence, the collection is not mutually independent.

---

> [!question] Problem 45
> A new treatment for a disease is being tested, to see whether it is better than the standard treatment. The existing treatment is effective on 50% of patients. It is believed initially that there is a 2/3 chance that the new treatment is effective on 60% of patients, and a 1/3 chance that the new treatment is effective on 50% of patients. In a pilot study, the new treatment is given to 20 random patients, and is effective for 15 of them.
> **(a)** Given this information, what is the probability that the new treatment is better than the standard treatment?
> **(b)** A second study is done later, giving the new treatment to 20 new random patients. Given the results of the first study, what is the PMF for how many of the new patients the new treatment is effective on? (Letting $p$ be the answer to (a), your answer can be left in terms of $p$.)

**Solution (a)** Let $H_1$ be the hypothesis that the new treatment is effective on 60% of patients ($p = 0.6$), and $H_0$ be the hypothesis that it is effective on 50% of patients ($p = 0.5$). The prior probabilities are:
$$
P(H_1) = \frac{2}{3}, \quad P(H_0) = \frac{1}{3}
$$
Let $X$ be the number of successful treatments out of $n = 20$ patients. The likelihood under each hypothesis follows a Binomial distribution:

- $X \mid H_1 \sim \text{Bin}(20, 0.6) \implies P(X = 15 \mid H_1) = \binom{20}{15}(0.6)^{15}(0.4)^5$
    
- $X \mid H_0 \sim \text{Bin}(20, 0.5) \implies P(X = 15 \mid H_0) = \binom{20}{15}(0.5)^{20}$

Applying Bayes' rule:
$$
P(H_1 \mid X = 15) = \frac{P(X = 15 \mid H_1)P(H_1)}{P(X = 15 \mid H_1)P(H_1) + P(X = 15 \mid H_0)P(H_0)}
$$
Canceling the binomial coefficient $\binom{20}{15}$:
$$
P(H_1 \mid X = 15) = \frac{\frac{2}{3}(0.6)^{15}(0.4)^5}{\frac{2}{3}(0.6)^{15}(0.4)^5 + \frac{1}{3}(0.5)^{20}} = \frac{2(0.6)^{15}(0.4)^5}{2(0.6)^{15}(0.4)^5 + (0.5)^{20}}
$$
Dividing numerator and denominator by $(0.5)^{20}$:
$$
\frac{2(0.6)^{15}(0.4)^5}{(0.5)^{20}} = 2 \cdot (1.2)^{15} \cdot (0.8)^5 \approx 10.0971
$$
$$
P(H_1 \mid X = 15) = \frac{10.0971}{10.0971 + 1} \approx 0.9099
$$
In exact rational form:
$$
P(H_1 \mid X = 15) = \frac{2^{26} \cdot 3^{15}}{2^{26} \cdot 3^{15} + 5^{20}} = \frac{962,938,883,735,552}{1,058,306,315,376,177} \approx 0.910
$$

**Solution (b)** Let $Y$ be the number of successful treatments among the 20 new patients. Conditioned on the true effectiveness rate, $Y$ is conditionally independent of $X$:
- $Y \mid H_1 \sim \text{Bin}(20, 0.6)$
    
- $Y \mid H_0 \sim \text{Bin}(20, 0.5)$

By the Law of Total Probability, for any $k \in \{0, 1, 2, \dots, 20\}$:
$$
P(Y = k \mid X = 15) = P(Y = k \mid H_1)P(H_1 \mid X = 15) + P(Y = k \mid H_0)P(H_0 \mid X = 15)
$$
Letting $p = P(H_1 \mid X = 15) \approx 0.91$:
$$
P(Y = k) = p \binom{20}{k}(0.6)^k(0.4)^{20-k} + (1 - p)\binom{20}{k}(0.5)^{20}, \quad k \in \{0, 1, \dots, 20\}
$$

---

> [!question] Problem 46
> 
> Independent Bernoulli trials are performed, with success probability 1/2 for each trial. An important question that often comes up in such settings is how many trials to perform. Many controversies have arisen in statistics over the issue of how to analyze data coming from an experiment where the number of trials can depend on the data collected so far.
> 
> For example, if we can follow the rule “keep performing trials until there are more than twice as many failures as successes, and then stop”, then naively looking at the ratio of failures to successes (if and when the process stops) will give more than 2:1 rather than the true theoretical 1:1 ratio; this could be a very misleading result! However, it might never happen that there are more than twice as many failures as successes; in this problem, you will find the probability of that happening.
> 
> **(a)** Two gamblers, A and B, make a series of bets, where each has probability 1/2 of winning a bet, but A gets \$2 for each win and loses \$1 for each loss (a very favorable game for A!). Assume that the gamblers are allowed to borrow money, so they can and do gamble forever. Let $p_k$ be the probability that A, starting with \$$k$, will ever reach \$$0$, for each $k \ge 0$. Explain how this story relates to the original problem, and how the original problem can be solved if we can find $p_k$.
> 
> **(b)** Find $p_k$.
> 
> _Hint:_ As in the gambler’s ruin, set up and solve a difference equation for $p_k$. We have $p_k \to 0$ as $k \to \infty$ (you don’t need to prove this, but it should make sense since the game is so favorable to A, which will result in A’s fortune going to $\infty$; a formal proof, not required here, could be done using the law of large numbers, an important theorem from Chapter 10). The solution can be written neatly in terms of the golden ratio.
>  
> **(c)** Find the probability of ever having more than twice as many failures as successes with independent $\text{Bern}(1/2)$ trials, as originally desired.

**Solution (a)** Let $S$ be the total number of successes and $F$ be the total number of failures. The stopping condition is:
$$
F > 2S \iff F - 2S \ge 1
$$
Consider Gambler A starting with a fortune of $1$ ($k = 1$\$). After $S$ successes and $F$ failures, A's total fortune is:
$$
X = 1 + 2S - F
$$
Because A's fortune increases by \$2 on each success and decreases by only \$1 on each failure, the fortune moves down in steps of size 1. It cannot jump over 0 to reach negative values without hitting 0 first. Thus, the very first moment $X \le 0$ must occur when $X = 0$:
$$
1 + 2S - F = 0 \iff F - 2S = 1
$$
This corresponds exactly to the first time $F > 2S$. Therefore, the probability that the stopping condition is ever reached equals the probability $p_1$ that Gambler A, starting with $1, ever hits $0.

**Solution (b)** Conditioning on the outcome of the first round for Gambler A starting at $k$:
$$
p_k = \frac{1}{2} p_{k+2} + \frac{1}{2} p_{k-1} \quad \text{for } k \ge 1
$$
Because down-steps can only occur one unit at a time, to transition from state $k$ to state 0, the random walk must step from $k$ to $k-1$, from $k-1$ to $k-2$, and so on down to 0. Each of these $k$ intermediate stages is independent and identical to transitioning from 1 to 0. Hence:
$$
p_k = (p_1)^k
$$
Substituting $p_k = x^k$ into the recurrence relation:
$$
x^k = \frac{1}{2} x^{k+2} + \frac{1}{2} x^{k-1}
$$
Dividing across by $\frac{1}{2} x^{k-1}$ gives:
$$
x^3 - 2x + 1 = 0
$$
Factoring out $(x - 1)$:
$$
(x - 1)(x^2 + x - 1) = 0
$$
The three roots are:
$$
x_1 = 1, \quad x_2 = \frac{-1 + \sqrt{5}}{2}, \quad x_3 = \frac{-1 - \sqrt{5}}{2}
$$
Expressing these roots using the golden ratio $\phi = \frac{1 + \sqrt{5}}{2} \approx 1.618$:
$$
x_2 = \phi - 1 = \frac{1}{\phi} \approx 0.618, \quad x_3 = -\phi \approx -1.618
$$
Applying physical constraints and boundary conditions:

1. $p_k$ represents a probability, so $0 \le p_k \le 1$ for all $k \ge 0$. This eliminates $x_3 = -\phi < -1$.
    
2. As $k \to \infty$, $p_k \to 0$. The root $x_1 = 1$ would imply $p_k = 1$ for all $k$, which violates this condition.
    
3. The root $x_2 = 1/\phi$ satisfies $0 < x_2 < 1$.

Given $p_0 = 1$:
$$
p_k = \left(\frac{\sqrt{5} - 1}{2}\right)^k = \phi^{-k}
$$
**Solution (c)** From part (a), the probability of ever stopping (having more than twice as many failures as successes) is:
$$
p_1 = \frac{\sqrt{5} - 1}{2} = \frac{1}{\phi} \approx 0.618 \quad (61.8\%)
$$

---

> [!question] Problem 47
> 
> A copy machine is used to make $n$ pages of copies per day. The machine has two trays in which paper gets loaded, and each page used is taken randomly and independently from one of the trays. At the beginning of the day, the trays are refilled so that they each have $m$ pages.
> 
> **(a)** Let $\text{pbinom}(x, n, p)$ be the CDF of the $\text{Bin}(n, p)$ distribution, evaluated at $x$. In terms of $\text{pbinom}$, find a simple expression for the probability that both trays have enough paper on any particular day, when this probability is strictly between 0 and 1 (also specify the values of $m$ for which the probability is 0 and the values for which it is 1).
> 
> _Hint:_ Be careful about whether inequalities are strict, since the Binomial is discrete.
> 
> **(b)** Using a computer, find the smallest value of $m$ for which there is at least a 95% chance that both trays have enough paper on a particular day, for $n = 10$, $n = 100$, $n = 1000$, and $n = 10000$.
> 
> _Hint:_ If you use R, you may find the following commands useful:
> `g <- function(m,n) [your answer from (a)]` defines a function `g` such that `g(m, n)` is your answer from (a), `g(1:100,100)` gives the vector `(g(1, 100), . . . , g(100, 100))`, `which(v>0.95)` gives the indices of the components of vector `v` that exceed 0.95, and `min(w)` gives the minimum of a vector `w`.
> 
>   

**Solution (a)** Let $X$ be the number of pages drawn from Tray 1 throughout the day. Then $X \sim \text{Bin}(n, 0.5)$, and the number of pages drawn from Tray 2 is $n - X$.

Both trays have sufficient paper if and only if:
$$
X \le m \quad \text{and} \quad n - X \le m \iff n - m \le X \le m
$$
- **Probability equals 0:** When $2m < n$ (i.e., $m < \lceil n/2 \rceil$), the combined capacity of both trays is strictly less than the $n$ pages required.
    
- **Probability equals 1:** When $m \ge n$, either tray can supply all $n$ pages entirely on its own.
    
- **Probability strictly between 0 and 1:** When $\lceil n/2 \rceil \le m < n$.

For $\lceil n/2 \rceil \le m < n$:
$$
P(n - m \le X \le m) = P(X \le m) - P(X \le n - m - 1)
$$
In terms of $\text{pbinom}$:
$$
P(n - m \le X \le m) = \text{pbinom}(m, n, 0.5) - \text{pbinom}(n - m - 1, n, 0.5)
$$
Using the symmetry of the $\text{Bin}(n, 0.5)$ distribution, $P(X \le n - m - 1) = P(X \ge m + 1) = 1 - P(X \le m)$, which simplifies to:
$$
2 \cdot \text{pbinom}(m, n, 0.5) - 1
$$

**Solution (b)** We require the minimum integer $m$ such that:
$$2 \cdot \text{pbinom}(m, n, 0.5) - 1 \ge 0.95 \iff \text{pbinom}(m, n, 0.5) \ge 0.975$$
Evaluating for each value of $n$:

| **n**  | **Smallest m** | **Success Probability**                                       |
| ------ | -------------- | ------------------------------------------------------------- |
| 10     | **8**          | $2 \cdot \text{pbinom}(8, 10, 0.5) - 1 \approx 97.85\%$       |
| 100    | **60**         | $2 \cdot \text{pbinom}(60, 100, 0.5) - 1 \approx 96.48\%$     |
| 1,000  | **531**        | $2 \cdot \text{pbinom}(531, 1000, 0.5) - 1 \approx 95.36\%$   |
| 10,000 | **5,098**      | $2 \cdot \text{pbinom}(5098, 10000, 0.5) - 1 \approx 95.12\%$ |

---

# Pure Skeleton Index — Random Variables

### 3.1 Random Variables

- Recall the two stated limitations of pure event/set notation that motivate random variables
- **Gambler's Ruin Motivating Example**: recall each derived quantity and how it's built from $X_k$ — wealth of B, wealth difference, wealth in euros, game duration $R$ (as a min/stopping-time definition)
##### Definition of Random Variable
- Define a random variable formally (domain, codomain, mapping)
- Notation convention (capital letters)
- Explain precisely where the randomness in $X(s)$ actually comes from (is $X$ itself random?)
- Pebble metaphor: describe what an r.v. does to the pebbles
##### Timing & Crystallization
- Describe the status/interpretation of $X$ *before* the experiment vs. *after* — what can be computed in each phase?
#### Example: Two Fair Coin Tosses
- Define $X$ (number of heads) on all 4 outcomes — reconstruct the mapping yourself
- Define $Y$ (number of tails) via $X$
- Define an Indicator r.v. $I$ (first toss heads) — general definition of an indicator r.v.
- Reconstruct the algebraic encoding of $X, Y, I$ using $(s_1,s_2)\in\{0,1\}^2$

### 3.2 Distributions and Probability Mass Functions

- Note the existence of hybrid r.v.s — recall the example given (coin-dependent discrete/continuous)
##### Discrete Random Variables
- Definition: what condition on $P(X\in\{a_1,\dots\})$ makes $X$ discrete?
- Definition: Support
- Contrast with Continuous r.v. (one line)
##### PMF
- Definition of $p_X(x)$
- State the two-part behavior of $p_X$ relative to the support

> Note: Explain precisely why $\{X=x\}$ is a valid object for $P(\cdot)$ but $P(X)$ alone is invalid — reconstruct the reasoning
##### Worked PMF Examples — reconstruct each PMF from the experiment setup (don't just recall the final numbers, derive the reasoning path)
- Two Fair Coin Tosses: $p_X$, $p_Y$, $p_I$
  - Key takeaway to explain: how can $X\neq Y$ yet $p_X = p_Y$?
- Sum of Two Fair Dice ($T=X+Y$):
  - Marginal PMFs of $X,Y$ (name the distribution family)
  - Reconstruct why $P(X=Y)=1/6$
  - Explain the distinction: same-distribution ($7-X$ vs $X$) vs. never-equal-in-a-single-roll — reconcile these two facts
  - Reconstruct the PMF shape (which values of $T$ are most/least likely, without necessarily listing every value)
  - State and justify the symmetry property $P(T=t)=P(T=14-t)$
- Children in US Households: what kind of external data determines this PMF (as opposed to being derived from a formula)?
##### Theorem: Criteria for a Valid PMF
- State both criteria precisely
- Proof of nonnegativity: what axiom does it follow from?
- Proof of "sums to 1": reconstruct the argument using disjointness of $\{X=x_j\}$ events
- State the converse claim
##### Calculating Probabilities of Subsets from PMF
- General formula for $P(X\in B)$
- Worked example structure ($P(1\le T\le 4)$ for two dice) — reconstruct which support values are relevant and why

### 3.3 Bernoulli and Binomial Distributions
##### Bernoulli Distribution
- Definition: $P(X=1)$, $P(X=0)$, notation, constraint on $p$
- Explain why both the distribution name AND the parameter are needed to fully specify it
- Bernoulli Trial "story" — define success/failure framing
- Definition: Indicator random variable $I_A$ and its distribution in terms of $P(A)$
##### Binomial Distribution
- State the "story" precisely (conditions: $n$, independence, constant $p$)
- Notation
- State the relationship $\text{Bern}(p)\equiv\text{Bin}(1,p)$
- Recall the three benefits of defining distributions via "story" (stated in the notes) rather than by formula alone
##### Theorem: Binomial PMF
- State the PMF formula structure and its support
- **Proof**: reconstruct via two steps — (1) probability of one specific ordered sequence with $k$ successes, (2) number of such sequences — and how they combine
- Explain why this is a valid PMF (which theorem does it invoke, and how does the binomial theorem confirm normalization?)
##### Properties and Shape of Binomial PMF
- What happens to symmetry when $p=1/2$ vs $p\neq1/2$?
- Explain the effect of larger/smaller $p$ on where the probability mass shifts
##### Success/Failure Duality
- Theorem: what is the distribution of $n-X$?
- **Story Proof**: reconstruct the "relabeling" argument
- **Algebraic Proof**: reconstruct via $P(Y=k)=P(X=n-k)$ and simplifying the binomial coefficient
##### Corollary: Symmetry at $p=1/2$
- State the symmetry claim about $P(X=n/2+j)$ vs $P(X=n/2-j)$
- Reconstruct the proof (uses the duality theorem above)
- **Worked Example (Two Coin Tosses revisited)**: identify the distributions of $X$, $Y$, $I$ in Binomial/Bernoulli notation, and explain why $X,Y$ share a distribution here

### 3.4 Hypergeometric Distribution

- Explain why sampling with replacement from an urn gives Binomial, but without replacement requires a new distribution — what breaks (independence)?
##### Story and Definition
- State Story 3.4.1 precisely (urn contents, sampling method, equal-likelihood assumption)
- Notation and meaning of each parameter $w,b,n$
##### Theorem: Hypergeometric PMF
- State the PMF formula structure and the valid range conditions on $k$
- **Proof**: reconstruct via naive-definition-of-probability counting (favorable samples / total samples) — explain when the count is impossible (state the two failure conditions)
- **Validity Check**: which combinatorial identity confirms normalization?

> Note: Explain why considering order (instead of unordered sets) gives the same answer — what cancels?
##### The Two-Tags Interpretation
- Describe the general "two sets of tags" framework abstractly
- What condition must hold about how at least one tag set is assigned?
- Reconstruct the mapping table (Story → 1st tag target/other → 2nd tag target/other → twice-tagged variable) for at least one of: Urn, Elk, Cards — do this from the framework, not by memorizing the table

> Note: Explain the etymology connection to hypergeometric series — what's the defining ratio-of-consecutive-terms property of a geometric series, and how does the PMF ratio for this distribution generalize it?
##### Applied Examples — set up (don't solve numerically) each as an instance of the two-tags framework
- Elk Capture-Recapture
- Aces in a Poker Hand
##### Theorem: Parameter Symmetry
- State the equivalence $\text{HGeom}(w,b,n)\equiv\text{HGeom}(n,w+b-n,w)$
- **Story Proof**: reconstruct by identifying what quantity both $X$ and $Y$ are secretly counting (twice-tagged items) under swapped tag roles
- **Algebraic Verification**: reconstruct the factorial expansion showing both PMFs match
##### Comparison: Binomial vs. Hypergeometric
- State similarities
- State the core difference (replacement/independence) and give the concrete illustrative reasoning (tagged elk example)

### 3.5 Discrete Uniform Distribution
##### Story and Definition
- State the story precisely
- Notation, and what the "parameter" is (note anything unusual about this parameter compared to Bernoulli/Binomial's numeric parameters)
##### PMF and Subset Probabilities
- State the PMF formula and connect it explicitly to the naive definition of probability
- State the subset-probability formula $P(X\in A)$
##### Case Study: Random Slips of Paper
- **Scenario 1 (With Replacement)**:
  - Reconstruct why the count of slips $\ge 80$ follows a Binomial — identify $n,p$
  - Reconstruct why each individual draw $X_j$ is Discrete Uniform — what symmetry argument justifies this?
  - Reconstruct the complement-based derivation for "100 drawn at least once"
- **Scenario 2 (Without Replacement)**:
  - Reconstruct why the count of slips $\ge 80$ is Hypergeometric — identify $w,b,n$
  - Reconstruct why each individual draw $Y_j$ is (unconditionally) Discrete Uniform, despite the draws being dependent — explain the "simultaneous draw" reframing that makes this clear
  - Reconstruct the disjoint-events argument for "100 drawn in the sample" probability
  - Recall the sanity-check reframing (slips fixed, labels random) and why it gives the same answer
##### Key Comparison
- State which probability (with vs. without replacement) is larger, and explain intuitively why
- State the generalization formula for $k$ draws without replacement, and the boundary check at $k=100$

### 3.6 Cumulative Distribution Functions

- Explain why the CDF is more broadly applicable than the PMF (which r.v. types is each defined for?)
- Definition: $F_X(x)$
##### Converting Between PMF and CDF
- PMF → CDF: state the summation formula
- Reconstruct the worked example structure ($X\sim\text{Bin}(4,1/2)$, evaluate at $x=1.5$) — explain why non-integer evaluation points still make sense
- CDF → PMF: describe the "flat regions + jumps" structure and what a jump height represents
##### Theorem: Properties of a Valid CDF
- State all three properties precisely (monotonicity, right-continuity, limits at $\pm\infty$)
- Note the plotting convention (open vs. closed dots) for right-continuity
##### Proof (for nonnegative-integer-support discrete r.v.)
- Property 1: reconstruct via event-subset argument and monotonicity of probability
- Property 2: reconstruct using the floor function — explain why $F$ is locally constant just right of an integer point
- Property 3: reconstruct both limits — one is immediate, the other requires summing the full PMF
##### Summary: Three Ways to Express a Distribution
- Name all three (PMF, CDF, Story) and state one distinguishing advantage of each

### 3.7 Functions of Random Variables

- General definition: what does applying $g:\mathbb{R}\to\mathbb{R}$ to an r.v. produce?
- **Basketball Example**: define $g$ and $h$ as described and explain what r.v. each produces (wins for B; indicator of series win)
##### Formal Definition and Composition
- Definition: precisely how is $g(X)$ built from $g$ and $X$ as functions on $S$?
- Explain the composition/crystallization relationship
#### Finding PMF Of $Y = g(X)$
##### Case 1: $g$ One-to-One
- State how probabilities transfer when $g$ is one-to-one
- State the general strategy this enables for finding unfamiliar PMFs
- **Worked Example: Random Walk Position**
	- Recall setup ($n$ steps, $\pm1$ each with prob $\frac{1}{2}$)
	- Define $X$ (steps right) and identify its distribution
	- Derive $Y$ as the position after $n$ steps as a function of $X$
	- Explain why $g(x)=2x-n$ being one-to-one lets you write $P(Y=k)$ directly — reconstruct the resulting support description and the PMF formula structure (which values of $k$ are achievable, and why the parity condition on $n+k$ matters)
##### Case 2: $g$ Not One-to-One
- State the general rule (summing over all $x$ with $g(x)=y$)
- State the theorem formally (support description + PMF formula)
- **Worked Example: Distance from Origin**
	- Define $D=|Y|$; explain why absolute value isn't one-to-one here
	- Reconstruct the case-by-case PMF derivation ($D=0$ vs $D=k>0$) using the symmetry $P(Y=k)=P(Y=-k)$
##### Functions of Multiple Random Variables
- Definition: $g(X,Y)$ as a function on the same sample space $S$
- Note the requirement when $X,Y$ are defined on different original sample spaces — what construction fixes this?
- Explain crystallization for two-variable functions (give the max/sum intuition)
##### Worked Example: Max of Two Dice
- Reconstruct the tabulation approach (36 equally likely pairs) conceptually
- Reconstruct the alternative decomposition method for $P(\max(X,Y)=5)$ — explain the logic of splitting into $X=5,Y\le4$ / $X\le4,Y=5$ / $X=5,Y=5$ and why the first two terms are equal
#### Category Errors and "Sympathetic Magic"
- Define "category error" in this context
- Define "sympathetic magic" specifically
- Recall the Korzybski quote's meaning as applied here
##### Pitfall 1: Multiplying a PMF by a Constant
- Explain the error (what goes wrong if you scale $p_j$ by 2)
- Explain the correction: is $2X$'s effect a horizontal or vertical transformation? On what values does it act?
##### Pitfall 2: Identical Distribution ⇏ Equality
- State the general misconception
- Toss Example 1: reconstruct why $X,Y\sim\text{Bern}(1/2)$ yet $P(X=Y)=0$
- Toss Example 2: reconstruct why $P(Z=X)=1/2\neq1$ despite $Z\sim\text{Bern}(1/2)$ too

### 3.8 Independence of Random Variables

- General definition (informal): what does independence mean for two r.v.s?
##### Formal Definitions
- Definition (two r.v.s): state the CDF-based product condition
- Discrete equivalent: state the PMF-based product condition
- Definition (many r.v.s): state the joint CDF product condition for $n$ variables
- State the condition for independence of an infinite collection
##### Contrast with Event Independence
- Explain why r.v. independence needs "only one equation" yet is actually stronger/harder to verify than event independence's $2^n-n-1$ conditions — what makes the single equation still comprehensive?
- State the relationship between mutual and pairwise independence for r.v.s (which implies which, and which direction fails)

> Note: reconstruct the limiting argument showing how the single product equation recovers pairwise (and higher) independence by sending some variables' bounds to infinity — explain why $\lim_{x\to\infty}P(X\le x)=1$ is the key tool, and walk through the LHS/RHS limit computation yourself
##### Worked Example: Sum and Difference of Two Dice
- Reconstruct why $X+Y$ and $X-Y$ are dependent — construct the specific probability comparison that demonstrates it
##### Theorem: Functions of Independent R.V.s
- State the theorem (function of $X$ independent of function of $Y$, given $X\perp Y$)
##### Independent and Identically Distributed (i.i.d.)
- Definition
- Explain why "independent" and "identically distributed" are orthogonal properties
- Reconstruct all four quadrant examples (Ind.+Id.Dist / Ind.+Not Id.Dist / Dep.+Id.Dist / Dep.+Not Id.Dist) — for each, identify a concrete example pair and justify why it belongs in that quadrant
#### Binomial Representations
##### Theorem: Sum of i.i.d. Bernoullis
- State the representation $X=X_1+\dots+X_n$
- Reconstruct the proof/justification in your own words (why does summing indicators count successes?)
##### Theorem: Sum of Independent Binomials
- State the claim ($X+Y\sim\text{Bin}(n+m,p)$ given independence and shared $p$)
- **Proof 1 (LOTP)**: reconstruct the conditioning-on-$X=j$ setup, the resulting double product of binomial PMFs, and which identity collapses the sum to the Binomial PMF
- **Proof 2 (Representation)**: reconstruct via writing both as sums of Bernoullis
- **Proof 3 (Story)**: reconstruct the one-line combined-trials argument
##### Conditional Independence and Conditional PMFs
- Definition: conditional independence of $X,Y$ given $Z$ (CDF form and discrete form)
- Definition: conditional PMF
#### Independence vs. Conditional Independence

Reconstruct each counterexample's setup and what's being compared (don't recall final probabilities, recall the mechanism)
##### Independence Does NOT Imply Conditional Independence
- Matching Pennies example: define $X,Y,Z$; explain why unconditional independence holds but conditioning on $Z=1$ creates dependence
- Two Friends example: define $X,Y,Z$; explain the deterministic relationship induced by conditioning
##### Conditional Independence Does NOT Imply Independence
- Mystery Opponent example: define $Z,X,Y$; explain why conditioning on $Z$ gives independence in each branch, but explain the mechanism by which observing $X$ provides information about which twin you're facing, thus creating unconditional dependence between $X$ and $Y$

### 3.9 Connections Between Binomial and Hypergeometric Distributions

- State the two fundamental connections (conditioning direction; limiting direction) in one line each
#### From Binomial to Hypergeometric: Conditioning (Fisher's Exact Test)
- Recall the two-group testing setup: define $X, Y$, their distributions, and the independence assumption
- State the null hypothesis precisely
- Recall the contingency-table framing (what's fixed: $n$, $m$, $r$)
##### Derivation of the Conditional PMF
- Set up $P(X=x\mid X+Y=r)$ via Bayes' rule — reconstruct the substitution of Binomial PMFs under the null hypothesis
- Reconstruct the algebraic cancellation that eliminates $p$ and $q$, leaving a ratio of binomial coefficients
- Identify the resulting distribution family
##### Connection to the Elk Story
- Reconstruct the mapping (women/men ↔ tagged/untagged elk; diseased ↔ recaptured)
##### Elimination of the Parameter $p$
- Explain why this is practically significant (what problem does it solve for a hypothesis test?)
#### Theorem: Conditioning Independent Binomials
- State the theorem precisely
#### From Hypergeometric to Binomial: Limiting Distribution
##### Theorem: Binomial as a Limit
- State the theorem precisely: what's held fixed ($p$), what goes to infinity ($N$)?
- **Proof**: reconstruct the proof outline —
  - Apply the Parameter Symmetry theorem from 3.4 as the starting move
  - Expand into factorial/product form
  - Divide by $N$ throughout — explain what this division accomplishes
  - Take the limit — identify what the numerator and denominator each converge to, and why
##### Story Intuition and Practical Approximation
- Explain in words why sampling without replacement approaches sampling with replacement as $N$ grows relative to $n$
- State the approximation condition ($N \gg n$) and what it means practically
- Recall the Birthday Problem connection example (1,200 from 1,000,000) — what does it illustrate about why the Binomial approximation still holds despite duplicates being non-negligible in absolute terms?

### Exercises — Reconstruct Each Solution From Scratch

**(25) $n$ vs. $n+1$ Coin Toss Symmetry**: $X\sim\text{Bin}(n,1/2)$, $Y\sim\text{Bin}(n+1,1/2)$, independent
- (a) Story approach: explain why $n-X$ and $n+1-Y$ share $X$'s and $Y$'s distributions respectively (symmetry of Binomial with $p=1/2$); state the resulting equality of events being compared
- (b) Reconstruct the simplification chain from $P(n-X<n+1-Y)$ down to $P(Y\le X)$, then use the complement relationship between $\{X<Y\}$ and $\{Y\le X\}$ to solve for $P(X<Y)$
- Algebraic approach (part a only): set up the LOTP expansion for both LHS and RHS as double sums; reconstruct the reindexing substitutions ($u=n-k$, $v=(n+1)-j$) that transform one sum into the other; note the alternative "matching terms" explanation via binomial symmetry

**(6) Benford's Law Validity**: State the two conditions for a valid PMF; reconstruct the log-positivity argument for condition 1; reconstruct the telescoping-sum argument for condition 2 using log subtraction properties

**(9) Mixture of CDFs**: $F=pF_1+(1-p)F_2$
- (a) Reconstruct each of the three CDF-validity proofs (monotonicity via weighted inequality preservation; limits via linearity of limits; right-continuity via linearity of limits) from the properties of $F_1,F_2$ individually
- (b) Set up the coin-flip generative mechanism with indicator $I$; reconstruct the LOTP derivation showing the resulting r.v.'s CDF equals $F$

**(33) Catching Independent Typos**: Define $X_1,X_2,X$ (typos caught by Prue, Frida, at least one)
- (a) Derive the "catch" probability $p^*$ via the complement (both miss); explain why $X\sim\text{Bin}(n,p^*)$ follows from independence across typos
- (b) With $p_1=p_2=p$: identify distributions of $X_1,X_2,X_1+X_2$; set up the conditional probability ratio $P(X_1=k\mid X_1+X_2=t)$ and simplify to identify the resulting named distribution and its parameters/support

**(34) Statistics Majors Sample**: $X\sim\text{Bin}(n,p)$ majors among $n$ students; sample of size $m$ without replacement
- (a) Set up LOTP conditioning on $X=k$, using the Hypergeometric conditional distribution of $Y$ given $X=k$; state the support of $Y$ and the summation range for $k$
- (b) Story proof: reconstruct the "does order of declaration matter" argument — reframe as sampling $m$ students first, then having them independently declare; identify the resulting simplified distribution

**(37) Undetected Errors via Parity Check**: Define the parity-check condition and connect it to $K\sim\text{Bin}(n,p)$ (number of bit errors) being nonzero and even
- (a) For $n=5,p=0.1$: set up the sum over even $k\ge2$ terms (structure, not final decimal)
- (b) State the general sum expression
- (c) Reconstruct the trick using $a+b=1$ (binomial theorem at $x=1$) and $a-b=(1-2p)^n$ (binomial theorem at $x=-1$) to solve for $a=P(K\text{ even})$; then subtract $P(K=0)$ to isolate the undetected-error probability

**(42) Shifted Day of the Week**: Define $X$ (uniform day) and $Y$ (next day, with wraparound)
- Determine whether $X,Y$ share a distribution — reconstruct the case-by-case check including the wraparound case ($X=7\to Y=1$)
- Determine $P(X<Y)$ — identify which values of $X$ satisfy the condition and which single value breaks it

**(Unnumbered — Identically Distributed & Order Probabilities)**:
- (a) For $p=0.9, 0.99, 0.9999999999999$: construct the generalized cyclic-shift example (Uniform on $\{1,\dots,n\}$ with wraparound) and determine the right $n$ for each target probability
- For $p=1$: reconstruct the proof of impossibility using a bounded strictly increasing function $g$ and an expectation-equality contradiction — explain each step (why $E[g(Y)-g(X)]>0$ from the strict inequality, why $E[g(Y)]=E[g(X)]$ from identical distributions)
- (b) With independence added: reconstruct the symmetry argument $P(X<Y)=P(Y<X)$ and the resulting bound $P(X<Y)\le1/2$; explain why this changes every answer from part (a)

**(44) XOR of Bernoullis**:
- (a) $X\sim\text{Bern}(p)$, $Y\sim\text{Bern}(1/2)$ independent: set up LOTP conditioning on $X$ to find the distribution of $X\oplus Y$ — reconstruct why the $p$-dependence cancels
- (b) Test independence of $X\oplus Y$ with $X$: reconstruct the conditional probability check across both values of $X$ — does it depend on $p$? Test independence with $Y$: reconstruct the analogous check — under what condition on $p$ does it hold?
- (c) $Y_J=\bigoplus_{j\in J}X_j$ for i.i.d. $\text{Bern}(1/2)$ variables:
  - Reconstruct the induction argument on $|J|$ showing $Y_J\sim\text{Bern}(1/2)$
  - Reconstruct the pairwise independence argument — split into disjoint-$J,K$ case and overlapping case (isolate an index unique to one set and apply part (b)'s logic)
  - Reconstruct the mutual-independence counterexample using three specific subsets whose XORs are algebraically dependent — explain why this breaks mutual (but not pairwise) independence

**(45) New Treatment Bayesian Update**: Prior $P(H_1)=2/3$ (60% effective), $P(H_0)=1/3$ (50% effective); 15/20 successes observed
- (a) Set up Bayes' rule with Binomial likelihoods under each hypothesis; reconstruct the cancellation of the shared binomial coefficient; reconstruct the simplification to a ratio of prime-power terms
- (b) Explain why $Y$ (second study's successes) is conditionally independent of $X$ given the hypothesis; set up the LOTP mixture formula for $P(Y=k\mid X=15)$ in terms of $p=P(H_1\mid X=15)$ from part (a)

**(46) Favorable Game / Golden Ratio Gambler's Ruin**: A wins $2 per success, loses $1 per failure, $p=1/2$
- (a) Define the stopping condition $F>2S$; reconstruct the reframing as Gambler A's fortune $X=1+2S-F$ hitting 0; explain why the discreteness of the walk (step size 1 on the down side) guarantees hitting exactly 0 rather than jumping past it; connect $p_1$ to the original problem's answer
- (b) Set up the first-step-conditioning recurrence for $p_k$; reconstruct why $p_k=(p_1)^k$ (chaining independent stages); substitute to get a cubic characteristic equation; factor out the obvious root; recall the two remaining roots in terms of $\sqrt5$/golden ratio; apply the boundary reasoning (probability bounds, limiting behavior as $k\to\infty$) to eliminate two of the three roots
- (c) Connect back to the final numeric answer's meaning in terms of $\phi$

**(47) Copy Machine Paper Trays**: $n$ pages/day, 2 trays each refilled to $m$, page source chosen randomly per page
- (a) Define $X\sim\text{Bin}(n,0.5)$ (pages from Tray 1); reconstruct the "both trays sufficient" condition as a double inequality on $X$; identify the boundary values of $m$ giving probability exactly 0 or exactly 1; for the strictly-between range, reconstruct the CDF-difference expression and the symmetry simplification using $\text{pbinom}$
- (b) Explain the search strategy (smallest $m$ hitting a 95% threshold) conceptually — what inequality on $\text{pbinom}$ are you solving for each $n$?